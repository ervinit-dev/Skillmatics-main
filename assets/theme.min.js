$(".mmpanels").on("touchmove", function (t) {
  t.preventDefault();
}),
  $(".mm-fullscreen-bg").on("touchmove", function (t) {
    t.preventDefault();
  }),
  $(document).scroll(function () {
    0 == $(document).scrollTop()
      ? ($(".offer-banner-revised").show(),
        $(".offer-banner-revised").css("transition", "ease-in 2s"),
        $(".secondary-submenu").css(
          "box-shadow",
          "inset -2px -1px 1px 1px #e6e9eb"
        ))
      : ($(".offer-banner-revised").hide(),
        $(".offer-banner-revised").css("transition", "ease-in 2s"),
        $(".secondary-submenu").css(
          "box-shadow",
          "inset -2px -1px 1px 1px #e6e9eb"
        ));
  }),
  setInterval(function () {
    $(".mmitemopen").length > 0
      ? ($(".tt-header-row").css("background-color", "rgba(0, 0, 0, 0.55)"),
        $("header .tt-cart .tt-dropdown-toggle .tt-badge-cart").css(
          "background",
          "none"
        ),
        $("header .tt-cart .tt-dropdown-toggle .tt-badge-cart").css(
          "color",
          "gray"
        ),
        $(".tt-mobile-parent-cart.tt-parent-box").css("pointer-events", "none"),
        $(".tt-mobile-parent-search.tt-parent-box").css(
          "pointer-events",
          "none"
        ))
      : ($(".tt-header-row").css("background-color", "white"),
        $("header .tt-cart .tt-dropdown-toggle .tt-badge-cart").css(
          "background",
          "#2484c6"
        ),
        $("header .tt-cart .tt-dropdown-toggle .tt-badge-cart").css(
          "color",
          "white"
        ),
        $(".tt-mobile-parent-cart.tt-parent-box").css("pointer-events", "all"),
        $(".tt-mobile-parent-search.tt-parent-box").css(
          "pointer-events",
          "all"
        ));
  }, 100),
  $(document).ready(function () {
    ($("#mm0 ul li")[7].style.paddingTop = "2rem"),
      ($("#mm0 ul li a")[7].style.fontFamily = "Avenir"),
      ($("#mm0 ul li a")[8].style.fontFamily = "Avenir"),
      ($("#mm0 ul li a")[9].style.fontFamily = "Avenir"),
      ($("#mm0 ul li a")[7].style.fontWeight = "400"),
      ($("#mm0 ul li a")[8].style.fontWeight = "400"),
      ($("#mm0 ul li a")[9].style.fontWeight = "400"),
      ($("#mm0 ul li a")[7].style.padding = "8px 16px"),
      ($("#mm0 ul li a")[8].style.padding = "8px 16px"),
      ($("#mm0 ul li a")[9].style.padding = "8px 16px"),
      ($("#mm1 ul li a")[1].style.fontSize = "20px"),
      ($("#mm1 ul li a")[1].style.fontWeight = "500"),
      ($("#mm1 ul li a")[1].style.fontFamily = "Mikado Regular"),
      ($("#mm2 ul li a")[1].style.fontSize = "20px"),
      ($("#mm2 ul li a")[1].style.fontWeight = "500"),
      ($("#mm2 ul li a")[1].style.fontFamily = "Mikado Regular");
  });
var $,
  $document = $(document),
  $window = $(window),
  $body = $("body"),
  $html = $("html"),
  $ttPageContent = $("#tt-pageContent"),
  $ttFooter = $("footer"),
  $ttHeader = $("header"),
  $ttLeftColumnAside = $ttPageContent.find(".leftColumn.aside"),
  $ttFilterOptions = $ttPageContent.find(".tt-filters-options"),
  header_menu_timeout = 200,
  header_menu_delay = 200,
  $ttTopPanel = $(".tt-top-panel"),
  $stucknav = $ttHeader.find(".tt-stuck-nav"),
  $ttDesctopMenu = $ttHeader.find(".tt-desctop-menu"),
  $ttDesctopParentMenu = $ttHeader.find(".tt-desctop-parent-menu"),
  $ttMobileParentMenu = $ttHeader.find(".tt-mobile-parent-menu"),
  $ttMobileParentMenuChildren = $ttMobileParentMenu.children(),
  $ttStuckParentMenu = $ttHeader.find(".tt-stuck-parent-menu"),
  $ttSearchObj = $ttHeader.find(".tt-search"),
  $ttDesctopParentSearch = $ttHeader.find(".tt-desctop-parent-search"),
  $ttMobileParentSearch = $ttHeader.find(".tt-mobile-parent-search"),
  $ttStuckParentSearch = $ttHeader.find(".tt-stuck-parent-search"),
  $ttcartObj = $ttHeader.find(".tt-cart"),
  $ttDesctopParentCart = $ttHeader.find(".tt-desctop-parent-cart"),
  $ttMobileParentCart = $ttHeader.find(".tt-mobile-parent-cart"),
  $ttStuckParentCart = $ttHeader.find(".tt-stuck-parent-cart"),
  $ttAccountObj = $ttHeader.find(".tt-account"),
  $ttDesctopParentAccount = $ttHeader.find(".tt-desctop-parent-account"),
  $ttMobileParentAccount = $ttHeader.find(".tt-mobile-parent-account"),
  $ttStuckParentAccount = $ttHeader.find(".tt-stuck-parent-account"),
  $ttMultiObj = $ttHeader.find(".tt-multi-obj"),
  $ttDesctopParentMulti = $ttHeader.find(".tt-desctop-parent-multi"),
  $ttMobileParentMulti = $ttHeader.find(".tt-mobile-parent-multi"),
  $ttStuckParentMulti = $ttHeader.find(".tt-stuck-parent-multi"),
  blocks = {
    ttCalendarDatepicker: $ttPageContent.find(".calendarDatepicker"),
    ttSliderBlog: $ttPageContent.find(".tt-slider-blog"),
    ttSlickMain: $ttPageContent.find(".tt-slick-main"),
    ttSliderBlogSingle: $ttPageContent.find(".tt-slider-blog-single"),
    ttVideoBlock: $(".tt-video-block"),
    ttBlogMasonry: $ttPageContent.find(".tt-blog-masonry"),
    ttPortfolioMasonry: $ttPageContent.find(".tt-portfolio-masonry"),
    ttProductMasonry: $ttPageContent.find(".tt-product-listing-masonry"),
    ttInputCounter: $(".tt-input-counter"),
    ttCollapseBlock: $(".tt-collapse-block"),
    modalVideoProduct: $("#modalVideoProduct"),
    modalAddToCart: $("#modalAddToCartProduct"),
    ttMobileProductSlider: $(".tt-mobile-product-slider"),
    ttCollapse: $ttPageContent.find(".tt-collapse"),
    ttProductListing: $ttPageContent.find(".tt-product-listing"),
    ttBtnColumnClose: $ttLeftColumnAside.find(".tt-btn-col-close"),
    ttBtnToggle: $ttFilterOptions.find(".tt-btn-toggle a"),
    ttBtnAddProduct: $ttPageContent.find(".tt_product_showmore"),
    ttProductItem: $ttPageContent.find(".tt-product, .tt-product-design02"),
    ttProductDesign02: $ttPageContent.find(".tt-product-design02"),
    ttProductDesign01: $ttPageContent.find(".tt-product"),
    ttFilterDetachOption: $ttLeftColumnAside.find(".tt-filter-detach-option"),
    ttFilterSort: $ttFilterOptions.find(".tt-sort"),
    ttShopCart: $ttPageContent.find(
      ".tt-shopcart-table, .tt-shopcart-table-02"
    ),
    ttSliderLookbook: $ttPageContent.find(".tt-slider-lookbook"),
    ttPortfolioContent: $ttPageContent.find(".tt-portfolio-content"),
    ttLookbook: $ttPageContent.find(".tt-lookbook"),
    ttAirSticky: $ttPageContent.find(".airSticky"),
    ttfooterMobileCollapse: $ttFooter.find(".tt-collapse-title"),
    ttBackToTop: $(".tt-back-to-top"),
    ttHeaderDropdown: $ttHeader.find(".tt-dropdown-obj"),
    mobileMenuToggle: $(".tt-menu-toggle"),
    ttCarouselProducts: $(".tt-carousel-products"),
    ttItemsCategories: $ttPageContent.find(".tt-items-categories"),
    ttDotsAbsolute: $ttPageContent.find(".tt-dots-absolute"),
    ttAlignmentImg: $ttPageContent.find(".tt-alignment-img"),
    ttProductSingleBtnZomm: $ttPageContent.find(
      ".tt-product-single-img .tt-btn-zomm"
    ),
    ttRevolutionPromo: $ttPageContent.find(".tt-revolution-promo"),
  },
  ttwindowWidth = window.innerWidth || $window.width();
if (
  (blocks.ttRevolutionPromo.length &&
    (setTimeout(function () {
      blocks.ttRevolutionPromo.fadeTo("90", 1);
    }, 2300),
    blocks.ttRevolutionPromo.on("click", ".tt-btn-close", function () {
      $(this).closest(".tt-revolution-promo").hide();
    })),
  blocks.ttItemsCategories.length && ttItemsCategories(),
  blocks.modalAddToCart.length && modalAddToCart(),
  $(".mainmenumob-js").length &&
    $(".mainmenumob-js").initMM({
      enable_breakpoint: !0,
      mobile_button: !0,
      breakpoint: 1025,
      menu_class: "mobile-main-menu",
      close_button_name: b_close,
      back_button_name: b_back,
    }),
  $ttTopPanel.length && ttTopPanel(),
  blocks.ttBackToTop.length && ttBackToTop(),
  $ttLeftColumnAside &&
    blocks.ttBtnColumnClose &&
    blocks.ttBtnToggle &&
    ttToggleCol(),
  blocks.ttCollapse.length && ttCollapse(),
  blocks.modalVideoProduct.length && ttVideoPopup(),
  blocks.ttCalendarDatepicker.length &&
    blocks.ttCalendarDatepicker.datepicker(),
  blocks.ttVideoBlock.length && ttVideoBlock(),
  -1 !== getInternetExplorerVersion() && $html.addClass("ie"),
  blocks.ttInputCounter.length && ttInputCounter(),
  $ttDesctopParentSearch.length && mobileParentSearch(),
  $ttcartObj.length && mobileParentCart(),
  $ttDesctopParentAccount.length && mobileParentAccount(),
  $ttDesctopParentMulti.length && mobileParentMulti(),
  (blocks.ttProductDesign01.length || $(".tt-product-single-info").length) &&
    ttProductHover(),
  blocks.ttfooterMobileCollapse.length && ttFooterCollapse(),
  blocks.ttLookbook.length && ttLookbook(ttwindowWidth),
  blocks.ttShopCart.length && ttShopCart(ttwindowWidth),
  blocks.ttCarouselProducts.length &&
    blocks.ttCarouselProducts.each(function () {
      var t = $(this),
        e = $(this).data("item");
      t.slick({
        dots: !1,
        arrows: !0,
        infinite: !0,
        speed: 300,
        slidesToShow: e || 4,
        slidesToScroll: e || 4,
        adaptiveHeight: !0,
        responsive: [
          {
            breakpoint: 1025,
            settings: { slidesToShow: 3, slidesToScroll: 3 },
          },
          { breakpoint: 791, settings: { slidesToShow: 2, slidesToScroll: 2 } },
        ],
      });
    }),
  blocks.ttSliderLookbook.length &&
    blocks.ttSliderLookbook.slick({
      dots: !0,
      arrows: !0,
      infinite: !0,
      speed: 300,
      slidesToShow: 1,
      adaptiveHeight: !0,
    }),
  $(document).ready(function () {
    blocks.ttMobileProductSlider.length &&
      (576 > $(window).width() ? t() : $(window).bind("resize.ttmobpr", t));
    function t() {
      $(window).unbind("resize.ttmobpr"),
        blocks.ttMobileProductSlider.on("init", function (t, e) {
          var t = new CustomEvent("mobile-slider-initialized", {
            detail: $(this),
          });
          window.dispatchEvent(t);
        });
      var t = "true" == blocks.ttMobileProductSlider.attr("data-slickdots");
      blocks.ttMobileProductSlider.slick({
        lazyLoad: "progressive",
        dots: t,
        arrows: !0,
        infinite: !1,
        speed: 300,
        slidesToShow: 1,
        slidesToScroll: 1,
        adaptiveHeight: !0,
      });
    }
  }),
  blocks.ttSlickMain.length &&
    blocks.ttSlickMain.slick({
      dots: !0,
      arrows: !0,
      infinite: !0,
      speed: 300,
      slidesToShow: 1,
      adaptiveHeight: !0,
      responsive: [{ breakpoint: 1025, settings: { dots: !1 } }],
    }),
  blocks.ttSliderBlog.length &&
    blocks.ttSliderBlog.slick({
      dots: !1,
      arrows: !0,
      infinite: !0,
      speed: 300,
      slidesToShow: 1,
      adaptiveHeight: !0,
    }),
  blocks.ttSliderBlogSingle.length)
) {
  blocks.ttSliderBlogSingle.slick({
    dots: !1,
    arrows: !1,
    infinite: !0,
    speed: 300,
    slidesToShow: 1,
    adaptiveHeight: !0,
  });
  var t = $(".tt-slick-quantity");
  t.length &&
    (t
      .find(".total")
      .html(blocks.ttSliderBlogSingle.slick("getSlick").slideCount),
    blocks.ttSliderBlogSingle.on("afterChange", function (e, n, o) {
      var a = $(".slick-current").attr("data-slick-index");
      a++, t.find(".account-number").html(a);
    }));
  var e = $(".tt-slick-button");
  e.length &&
    (e.find(".slick-next").click(function () {
      blocks.ttSliderBlogSingle.slick("slickNext");
    }),
    e.find(".slick-prev").click(function () {
      blocks.ttSliderBlogSingle.slick("slickPrev");
    }));
}
$(".custom-slideshow").slick({
  dots: !0,
  infinite: !1,
  speed: 300,
  autoplay: !0,
  slidesToShow: 1,
  slidesToScroll: 1,
  responsive: [{ breakpoint: 480, settings: { dots: !0 } }],
}),
  $(".collectiontabscroll").slick({
    dots: !1,
    infinite: !1,
    speed: 300,
    slidesToShow: 4,
    slidesToScroll: 3,
    responsive: [
      {
        breakpoint: 1024,
        settings: {
          slidesToShow: 3,
          slidesToScroll: 2,
          infinite: !0,
          dots: !0,
        },
      },
      {
        breakpoint: 600,
        settings: { slidesToShow: 3, slidesToScroll: 2, dots: !0 },
      },
      {
        breakpoint: 480,
        settings: { slidesToShow: 2, slidesToScroll: 1, dots: !0 },
      },
    ],
  }),
  $(".responsive-collectongrid").slick({
    dots: !1,
    infinite: !1,
    speed: 300,
    slidesToShow: 6,
    slidesToScroll: 5,
    responsive: [
      {
        breakpoint: 1024,
        settings: {
          slidesToShow: 4,
          slidesToScroll: 3,
          infinite: !0,
          dots: !1,
        },
      },
      { breakpoint: 600, settings: { slidesToShow: 3, slidesToScroll: 2 } },
      { breakpoint: 480, settings: { slidesToShow: 3, slidesToScroll: 1 } },
    ],
  }),
  $(".responsive-brandlogo").slick({
    dots: !1,
    infinite: !0,
    speed: 300,
    slidesToShow: 4,
    slidesToScroll: 4,
    responsive: [
      {
        breakpoint: 1024,
        settings: {
          slidesToShow: 4,
          slidesToScroll: 3,
          infinite: !0,
          dots: !1,
        },
      },
      { breakpoint: 600, settings: { slidesToShow: 3, slidesToScroll: 1 } },
      { breakpoint: 480, settings: { slidesToShow: 3, slidesToScroll: 1 } },
    ],
  }),
  blocks.ttPortfolioContent.length &&
    is_touch_device() &&
    ttPortfolioContentMobile(),
  blocks.ttDotsAbsolute.length && ttSlickDots(),
  blocks.ttAirSticky.length && ttAirSticky(ttwindowWidth),
  blocks.ttHeaderDropdown.length && ttHeaderDropdown(),
  blocks.ttProductSingleBtnZomm.length && ttProductSingleBtnZomm(),
  $window.on("load", function () {
    var t = window.innerWidth || $window.width();
    $body.length && $body.addClass("loaded"),
      $ttFilterOptions.length && ttFilterLayout(t),
      blocks.ttProductItem.length && ttProductSmall(t),
      blocks.ttProductDesign02.length && ttOverflowProduct(),
      blocks.ttAlignmentImg.length && alignmentArrowValue(),
      blocks.ttProductMasonry.length && gridProductMasonr(),
      blocks.ttBlogMasonry.length && gridGalleryMasonr(),
      blocks.ttPortfolioMasonry.length &&
        (gridPortfolioMasonr(), initPortfolioPopup());
  });
var ttCachedWidth = $window.width();
$window.on("resize", function () {
  var t = $window.width();
  if (t !== ttCachedWidth) {
    ttCachedWidth = t;
    var e = window.innerWidth || $window.width();
    blocks.ttShopCart.length && ttShopCart(e),
      $ttFilterOptions.length && ttFilterLayout(e),
      blocks.ttProductItem.length && ttProductSmall(),
      blocks.ttProductDesign02.length && ttOverflowProduct(),
      blocks.ttPortfolioContent.length &&
        is_touch_device() &&
        ttPortfolioContentMobile(),
      blocks.ttAirSticky.length && ttAirSticky(e),
      $ttLeftColumnAside.hasClass("column-open") &&
        $ttLeftColumnAside.length &&
        $ttLeftColumnAside.find(".tt-btn-col-close a").trigger("click"),
      $ttDesctopParentSearch.length && mobileParentSearch(),
      $ttcartObj.length && mobileParentCart(),
      $ttDesctopParentAccount.length && mobileParentAccount(),
      $ttDesctopParentMulti.length && mobileParentMulti(),
      blocks.ttDotsAbsolute.length && ttSlickDots(),
      blocks.ttAlignmentImg.length && alignmentArrowValue();
  }
});
var cssFix = (function () {
  var t = navigator.userAgent.toLowerCase(),
    e = function (e) {
      return -1 != t.indexOf(e);
    };
  $html.addClass(
    [
      !/opera|webtv/i.test(t) && /msie (\d)/.test(t)
        ? "ie ie" + RegExp.$1
        : e("firefox/2")
        ? "gecko ff2"
        : e("firefox/3")
        ? "gecko ff3"
        : e("gecko/")
        ? "gecko"
        : e("opera/9")
        ? "opera opera9"
        : /opera (\d)/.test(t)
        ? "opera opera" + RegExp.$1
        : e("konqueror")
        ? "konqueror"
        : e("applewebkit/")
        ? "webkit safari"
        : e("mozilla/")
        ? "gecko"
        : "",
      e("x11") || e("linux")
        ? " linux"
        : e("mac")
        ? " mac"
        : e("win")
        ? " win"
        : "",
    ].join("")
  );
})();
function ttTopPanel() {
  $ttTopPanel.on("click", function (t) {
    t.preventDefault;
    var e = t.target;
    $(".tt-btn-close").is(e) && $(this).slideUp(200);
  });
}
function ttItemsCategories() {
  blocks.ttItemsCategories.hover(function () {
    $(this).toggleClass("active");
  });
}
function ttHeaderDropdown() {
  $(".header-popup-bg").length ||
    $body.append('<div class="header-popup-bg"></div>'),
    $("header").on("click", ".tt-dropdown-obj", function (t) {
      var e,
        n = window.innerWidth || $window.width(),
        o = $(this),
        a = t.target,
        s = $(".tt-search"),
        r = s.find(".tt-search-input");
      if (
        (o.hasClass("tt-search") &&
          $(".tt-dropdown-toggle").is(a) &&
          ($("body").addClass("tt-open-search"),
          o.addClass("active"),
          setTimeout(function () {
            r.focus();
          }, 150)),
        s.find(".tt-btn-close").is(a))
      )
        return (
          $("body").removeClass("tt-open-search"),
          o.removeClass("active"),
          r.blur(),
          !1
        );
      function c() {
        return (
          $(".tt-dropdown-obj.active").removeClass("active"),
          $(".tt-dropdown-obj.activetoview").length &&
            setTimeout(function () {
              $(".tt-dropdown-obj.activetoview").removeClass("activetoview");
            }, 200),
          $body.removeClass("tt-popup-dropdown"),
          !1
        );
      }
      !$(this).hasClass("tt-search") &&
        $(".ttt-dropdown-toggle").is(a) &&
        (n <= 1024
          ? ((e = o),
            $("header").find(".tt-dropdown-obj.active").removeClass("active"),
            e.toggleClass("activetoview"),
            setTimeout(function () {
              e
                .toggleClass("active")
                .find(".tt-dropdown-menu")
                .removeAttr("style"),
                $body.toggleClass("tt-popup-dropdown"),
                $("header .tt-cart .tt-dropdown-menu")
                  .perfectScrollbar()
                  .addClass("perfectScrollbar");
            }, 50))
          : (function e(n) {
              var o = n,
                a = t.target;
              if (o.hasClass("active")) {
                o
                  .toggleClass("active")
                  .find(".tt-dropdown-menu")
                  .slideToggle(200),
                  o.find(".ps-container").removeAttr("style");
                return;
              }
              if (
                ($(".tt-desktop-header .tt-dropdown-obj").each(function () {
                  var t = $(this);
                  t.hasClass("active") &&
                    t
                      .removeClass("active")
                      .find(".tt-dropdown-menu")
                      .css("display", "none");
                }),
                $(".tt-dropdown-toggle").is(a) &&
                  (function t(e) {
                    e.toggleClass("active")
                      .find(".tt-dropdown-menu")
                      .slideToggle(200);
                  })(o),
                o.hasClass("tt-cart"))
              ) {
                $.fn.getRealDimensions = function (t) {
                  var e = $(this);
                  if (0 == e.length) return !1;
                  var n = e
                      .clone()
                      .show()
                      .css("visibility", "hidden")
                      .insertAfter(e),
                    o = {
                      width: t ? n.outerWidth() : n.innerWidth(),
                      height: t ? n.outerHeight() : n.innerHeight(),
                      offsetTop: n.offset().top,
                      offsetLeft: n.offset().left,
                    };
                  return n.remove(), o;
                };
                var s = $(".tt-cart-content").getRealDimensions().height,
                  r = window.innerHeight,
                  c = s + $(".tt-desktop-header").height(),
                  d = $(".tt-stuck-nav");
                if (d.hasClass("stuck")) var h = r - d.height();
                else var h = r - $(".tt-desktop-header").height();
                r <= c &&
                  $("header .tt-dropdown-menu .tt-cart-layout")
                    .css({ height: h + "px" })
                    .perfectScrollbar()
                    .addClass("perfectScrollbar"),
                  $(window).resize(
                    debouncer(function (t) {
                      $(".tt-cart").hasClass("active") &&
                        $(".tt-cart .tt-dropdown-toggle").trigger("click");
                    })
                  );
              }
            })(o)),
        $(document).mouseup(function (t) {
          var e = window.innerWidth || $window.width();
          o.is(t.target) ||
            0 !== o.has(t.target).length ||
            o.each(function () {
              o.hasClass("active") &&
                o.hasClass("tt-search") &&
                s.find(".tt-btn-close").trigger("click"),
                o.hasClass("active") &&
                  !o.hasClass("tt-search") &&
                  (e <= 1024
                    ? c()
                    : $(".tt-dropdown-obj").each(function () {
                        $(this).hasClass("active") &&
                          $(this)
                            .removeClass("active")
                            .find(".tt-dropdown-menu")
                            .css("display", "none");
                      }));
            }),
            o.find(".tt-mobile-add .tt-close").is(t.target) && c();
        });
    });
}
function ttBackToTop() {
  blocks.ttBackToTop.on("click", function (t) {
    return $("html, body").animate({ scrollTop: 0 }, 500), !1;
  }),
    $window.scroll(function () {
      $window.scrollTop() > 500
        ? blocks.ttBackToTop.stop((!0).false).addClass("tt-show")
        : blocks.ttBackToTop.stop((!0).false).removeClass("tt-show");
    });
}
function modalAddToCart() {
  blocks.modalAddToCart.on("click", ".btn-close-popup", function (t) {
    return (
      $(this)
        .closest(".modal-content")
        .find(".modal-header .close")
        .trigger("click"),
      !1
    );
  });
}
function ttFooterCollapse() {
  blocks.ttfooterMobileCollapse.on("click", function (t) {
    t.preventDefault, $(this).toggleClass("tt-open");
  });
}
function ttSlickDots() {
  blocks.ttDotsAbsolute.each(function () {
    var t = $(this).find(".slick-dots");
    if (t.is(":visible")) {
      var e = t.closest("[class ^= container]");
      e.length &&
        e.css({
          paddingBottom:
            parseInt(t.height(), 10) + parseInt(t.css("marginTop"), 10),
        });
    }
  });
}
function ttProductHover() {
  $document.on(
    "mouseenter mouseleave",
    "#tt-pageContent .tt-product",
    function (t) {
      if (
        $(".tt-product-listing").length &&
        $(".tt-product-listing").hasClass("tt-col-one")
      )
        return !1;
      var e,
        n,
        o = $(this),
        a = window.innerWidth,
        s = o.find(".tt-description"),
        r = o.find(".tt-product-inside-hover"),
        c = parseInt(r.height()) + 3,
        d = o.find(".tt-countdown_box");
      if ((t.target, o.hasClass("product-nohover"))) return !1;
      "mouseenter" === t.type && a > 1024
        ? (o.stop().css({ height: o.innerHeight() }).addClass("hovered"),
          s.stop().animate({ top: "-" + c }, 200),
          r.stop().animate({ opacity: 1 }, 400),
          d.stop().animate({ bottom: c }, 200))
        : "mouseleave" === t.type &&
          t.relatedTarget &&
          a > 1024 &&
          (o.stop().removeClass("hovered").removeAttr("style"),
          s.stop().animate({ top: "0" }, 200, function () {
            $(this).removeAttr("style");
          }),
          r.stop().animate({ opacity: 0 }, 100, function () {
            $(this).removeAttr("style");
          }),
          d.stop().animate({ bottom: 0 }, 200, function () {
            $(this).removeAttr("style");
          }));
    }
  );
}
function ttShopCart(t) {
  var e = blocks.ttShopCart.find(".detach-quantity-desctope"),
    n = blocks.ttShopCart.find(".detach-quantity-mobile");
  t <= 789
    ? e.each(function () {
        var t = $(this).find(".tt-input-counter").detach().get(0);
        $(this).closest("tr").find(".detach-quantity-mobile").append(t);
      })
    : n.each(function () {
        var t = $(this).find(".tt-input-counter").detach().get(0);
        $(this).closest("tr").find(".detach-quantity-desctope").append(t);
      });
}
function ttProductSmall() {
  var t = parseInt(blocks.ttProductItem.width(), 10),
    e = $(".tt-product, .tt-product-design02");
  t <= 210 ? e.addClass("tt-small") : e.removeClass("tt-small");
}
function debouncer(t, e) {
  var n,
    e = e || 500;
  return function () {
    var o = this,
      a = arguments;
    clearTimeout(n),
      (n = setTimeout(function () {
        t.apply(o, Array.prototype.slice.call(a));
      }, e));
  };
}
function alignmentArrowValue() {
  if (!$ttPageContent.find(".tt-alignment-img").length) return !1;
  (window.innerWidth || $window.width()) > 1024
    ? setTimeout(function () {
        $ttPageContent.find(".tt-alignment-img").each(function () {
          $(this).find(".slick-arrow").removeAttr("style");
        });
      }, 225)
    : setTimeout(function () {
        $ttPageContent.find(".tt-alignment-img").each(function () {
          var t = $(this),
            e = t.find(".slick-arrow");
          if (0 != t.find(".tt-image-box").length && 0 != e.length) {
            var n = t.find(".tt-image-box").first();
            e.css({
              top:
                n.findHeight() -
                e.findHeight() -
                parseInt(t.css("marginTop"), 10) +
                "px",
            }),
              t.find(".tt-product").length && ttProductSmall();
          }
        });
      }, 225);
}
function ttLookbook(t) {
  $(".tt-lookbook-popup").length ||
    $body.append(
      '<div class="tt-lookbook-popup"><div class="tt-lookbook-container"></div></div>'
    );
  var e = "ontouchstart" in window ? "click" : "mouseenter";
  function n(t, e) {
    var n,
      o,
      a,
      s = $(t),
      r = (e.target, $(".tt-hotspot"), window.innerWidth || $window.width()),
      c = $(".tt-btn").innerHeight() / 2,
      d = $(".tt-hotspot-content").innerWidth();
    if (
      (r <= 789
        ? ((n = s),
          (o = n.attr("data-top") + "%"),
          (a = n.attr("data-left") + "%"),
          n.find(".tt-btn").css({ top: o, left: a }),
          n.css({ top: "0px", left: "0px", width: "100%", height: "100%" }),
          n.addClass("active").siblings().removeClass("active"),
          n.find(".tt-content-parent").fadeIn(200))
        : (function t(e) {
            if (!e.hasClass("active")) {
              var n,
                o,
                a,
                p,
                f = e.offset().top + c,
                g = e.offset().left,
                m = e.find(".tt-hotspot-content").detach();
              $(".tt-lookbook-container").children().size() > 0 &&
                (r <= 789 ? u() : h()),
                (n = m),
                (o = f),
                (p = 0),
                r / 2 < (a = g)
                  ? (function t(e) {
                      $(".tt-lookbook-popup")
                        .css({ top: o, left: e, display: "block" }, 300)
                        .animate({ marginLeft: "26px", opacity: 1 }, 300);
                    })((p = a - d - 7))
                  : (function t(e) {
                      $(".tt-lookbook-popup")
                        .css({ top: o, left: e, display: "block" })
                        .animate({ marginLeft: "-26px", opacity: 1 });
                    })((p = a + 45)),
                $(".tt-lookbook-popup")
                  .find(".tt-lookbook-container")
                  .append(n),
                s.addClass("active").siblings().removeClass("active");
            }
          })(s),
      r <= 789)
    ) {
      if ($(".tt-btn-close").is(e.target)) return e.preventDefault(), u(), !1;
      $(".tt-hotspot").is(e.target) && u(),
        $(document).mouseup(function (t) {
          $(".tt-lookbook-popup").is(t.target) ||
            0 !== $(".tt-lookbook-popup").has(t.target).length ||
            $(".tt-hotspot").is(t.target) ||
            0 !== $(".tt-hotspot").has(t.target).length ||
            h();
        });
    }
    function h() {
      var t = $(".tt-lookbook-popup")
        .removeAttr("style")
        .find(".tt-hotspot-content")
        .detach();
      $(".tt-hotspot.active")
        .removeClass("active")
        .find(".tt-content-parent")
        .append(t);
    }
    function u() {
      if ($(".tt-lookbook-container").is(":has(div)")) {
        var t = $(".tt-lookbook-container")
          .find(".tt-hotspot-content")
          .detach();
        $(".tt-hotspot.active").find(".tt-content-parent").append(t);
      }
      $(".tt-lookbook")
        .find(".tt-hotspot.active")
        .each(function (t) {
          var e = $(this),
            n = e.attr("data-top") + "%",
            o = e.attr("data-left") + "%";
          e.removeClass("active")
            .removeAttr("style")
            .css({ top: n, left: o })
            .find(".tt-btn")
            .removeAttr("style")
            .next()
            .removeAttr("style");
        });
    }
    r > 789 &&
      $(document).mouseup(function (t) {
        if (
          (window.innerWidth || $window.width(),
          $(".tt-btn-close").is(t.target))
        )
          return h(), !1;
        $(".tt-lookbook-popup").is(t.target) ||
          0 !== $(".tt-lookbook-popup").has(t.target).length ||
          $(".tt-hotspot").is(t.target) ||
          0 !== $(".tt-hotspot").has(t.target).length ||
          h();
      }),
      $(window).resize(
        debouncer(function (t) {
          789 >= (window.innerWidth || $window.width())
            ? u()
            : (h(),
              $(".tt-hotspot")
                .find(".tt-content-parent")
                .each(function () {
                  var t = $(this);
                  if ("block" == t.css("display")) {
                    var e = t.closest(".tt-hotspot"),
                      n = e.attr("data-top") + "%",
                      o = e.attr("data-left") + "%";
                    t.removeAttr("style").prev().removeAttr("style"),
                      e.removeAttr("style").css({ top: n, left: o });
                  }
                }));
        })
      );
  }
  blocks.ttLookbook.on(e, ".tt-hotspot", function (t) {
    var e, o, a, s;
    $(this).hasClass("emptycontent")
      ? ((e = this),
        (o = t),
        (a = $(e)),
        (s =
          "/products/" +
          $(e).attr("data-prhandle") +
          "?view=getproductlookbook"),
        a.removeClass("emptycontent"),
        jQuery.ajax({
          type: "get",
          url: s,
          success: function (t) {
            a.find(".tt-content-parent").append(t), setTimeout(n(e, o), 200);
          },
        }))
      : n(this, t);
  });
}
function ttOverflowProduct() {
  blocks.ttProductDesign02.hover(
    function () {
      if (
        $(".tt-product-listing").length &&
        $(".tt-product-listing").hasClass("tt-col-one")
      )
        return !1;
      if (!(window.innerWidth < 1024)) {
        var t = $(this).find(".tt-image-box").height(),
          e = $(this).find(".tt-description");
        t > e.height() + 25 ||
          ($(this).addClass("tt-small"), e.height(t).perfectScrollbar());
      }
    },
    function () {
      window.innerWidth < 1024 ||
        $(this)
          .removeClass("tt-small")
          .find(".tt-description")
          .removeAttr("style")
          .perfectScrollbar("destroy");
    }
  );
}
function ttReinitflowProduct() {
  window.innerWidth < 1024 ||
    0 == $ttPageContent.find(".tt-product-design02").length ||
    $ttPageContent.find(".tt-product-design02").each(function () {
      $(this).perfectScrollbar("destroy");
      var t = $(this).find(".tt-image-box").height(),
        e = $(this).find(".tt-description");
      t > e.height() + 25 ||
        ($(this).addClass("tt-small"), e.height(t).perfectScrollbar());
    });
}
function ttPortfolioContentMobile() {
  blocks.ttPortfolioContent.on("click", "figure img", function () {
    $(this)
      .closest(".tt-portfolio-content")
      .find("figure")
      .removeClass("gallery-click"),
      $(this).closest("figure").addClass("gallery-click");
  });
}
function ttToggleCol() {
  var t = $ttLeftColumnAside.find(".tt-btn-col-close a");
  $(".tt-btn-toggle a").on("click", function (e) {
    e.preventDefault();
    var n = $body.scrollTop() || $html.scrollTop();
    $ttLeftColumnAside.toggleClass("column-open").perfectScrollbar(),
      $body
        .css("top", -n)
        .addClass("no-scroll")
        .append('<div class="modal-filter"></div>');
    var o = $(".modal-filter").fadeTo("fast", 1);
    return (
      o.length &&
        o.click(function () {
          t.trigger("click");
        }),
      !1
    );
  }),
    blocks.ttBtnColumnClose.on("click", function (t) {
      t.preventDefault(),
        $ttLeftColumnAside
          .removeClass("column-open")
          .perfectScrollbar("destroy");
      var e = -1 * parseInt($body.css("top").replace("px", ""), 10);
      $body.removeAttr("style").removeClass("no-scroll").scrollTop(e),
        $html.removeAttr("style").scrollTop(e),
        $(".modal-filter").unbind().remove();
    });
}
function getInternetExplorerVersion() {
  var t = -1;
  if ("Microsoft Internet Explorer" === navigator.appName) {
    var e = navigator.userAgent,
      n = RegExp("MSIE ([0-9]{1,}[.0-9]{0,})");
    null != n.exec(e) && (t = parseFloat(RegExp.$1));
  } else if ("Netscape" === navigator.appName) {
    var e = navigator.userAgent,
      n = RegExp("Trident/.*rv:([0-9]{1,}[.0-9]{0,})");
    null != n.exec(e) && (t = parseFloat(RegExp.$1));
  }
  return t;
}
function is_touch_device() {
  return "ontouchstart" in window || "onmsgesturechange" in window;
}
function ttVideoBlock() {
  $(".tt-video-block").on("click", function (t) {
    t.preventDefault();
    var e = $(this).find(".movie")[0];
    e.paused
      ? (e.play(), $(this).addClass("play"))
      : (e.pause(), $(this).removeClass("play"));
  });
}
function gridGalleryMasonr() {
  var t = blocks.ttBlogMasonry
    .find(".tt-blog-init")
    .isotope({ itemSelector: ".element-item", layoutMode: "masonry" });
  t.imagesLoaded().progress(function () {
    t.isotope("layout").addClass("tt-show");
  });
  var e = blocks.ttBlogMasonry.find(".tt-filter-nav");
  if (e.length) {
    var n = {
      ium: function () {
        return $(this).find(".name").text().match(/ium$/);
      },
    };
    e.on("click", ".button", function () {
      var e = $(this).attr("data-filter");
      (e = n[e] || e),
        t.isotope({ filter: e }),
        $(this).addClass("active").siblings().removeClass("active");
    });
  }
}
function gridProductMasonr() {
  var t = blocks.ttProductMasonry
    .find(".tt-product-init")
    .isotope({ itemSelector: ".element-item", layoutMode: "masonry" });
  t.imagesLoaded().progress(function () {
    t.isotope("layout");
  });
  var e = blocks.ttProductMasonry.find(".tt-filter-nav");
  if (e.length) {
    var n = {
      ium: function () {
        return $(this).find(".name").text().match(/ium$/);
      },
    };
    e.on("click", ".button", function () {
      var e = $(this).attr("data-filter");
      (e = n[e] || e),
        t.isotope({ filter: e }),
        $(this).addClass("active").siblings().removeClass("active");
    });
  }
}
function ttCollapse() {
  var t = blocks.ttCollapse,
    e = t.find(".tt-collapse-title"),
    n = t.find(".tt-collapse-content");
  t.each(function () {
    $(this).hasClass("open")
      ? $(this).find(n).slideDown()
      : $(this).find(n).slideUp();
  }),
    e.on("click", function (t) {
      t.preventDefault();
      var e = $(this).parent(),
        n = $(this).next(".tt-collapse-content");
      e.hasClass("open")
        ? (e.removeClass("open"), n.slideUp(300))
        : (e.addClass("open"), n.slideDown(300));
    });
}
function ttFilterLayout(t) {
  if ($(".tt-product-listing").hasClass("only-row")) return !1;
  function e() {
    if (!blocks.ttFilterDetachOption.find(".filters-row-select").length)
      return !1;
    var t = blocks.ttFilterSort.find("select").detach();
    blocks.ttFilterDetachOption.find(".filters-row-select").append(t);
  }
  function n() {
    if (!blocks.ttFilterDetachOption.find(".filters-row-select").length)
      return !1;
    var t = blocks.ttFilterDetachOption
      .find(".filters-row-select select")
      .detach();
    blocks.ttFilterSort.append(t);
  }
  $ttFilterOptions.hasClass("desctop-no-sidebar") &&
    !$ttFilterOptions.hasClass("filters-detach-mobile") &&
    (t <= 0 ? e() : n()),
    $ttFilterOptions.hasClass("filters-detach-mobile") && (t <= 0 ? e() : n()),
    $ttFilterOptions.hasClass("desctop-no-sidebar") || (t <= 0 ? e() : n()),
    blocks.ttProductListing.removeClassFirstPart("tt-col-*");
  var o = $ttFilterOptions.find(".tt-quantity"),
    a = blocks.ttProductListing.find(".tt-col-item:first"),
    s = (function () {
      if (o.length && a.length)
        return (
          parseInt(a.css("flex").replace("0 0", "").replace("%", ""), 10) ||
          parseInt(a.css("max-width"), 10)
        );
    })();
  function r(e, n) {
    t <= 1024
      ? (function t(e, n) {
          e.find(".tt-show").removeClass("tt-show"),
            e.find(".tt-show-siblings").removeClass("tt-show-siblings");
          var o = e.find(n);
          if (
            (o.addClass("tt-show"),
            o.prev().addClass("tt-show-siblings"),
            "false" == $(".tt-product-listing").data("onerow"))
          )
            return !1;
          $(".tt-col-one").addClass("tt-show-siblings");
          var a = e.find(".active").data("value");
          $(".tt-product-listing").addClass(a);
        })(e, n)
      : (function t(e, n) {
          e.find(".tt-show").removeClass("tt-show"),
            e.find(".tt-show-siblings").removeClass("tt-show-siblings");
          var o = e.find(n);
          if (
            (o.addClass("tt-show"),
            o.next().addClass("tt-show-siblings"),
            o.prev().addClass("tt-show-siblings"),
            1 === $(".tt-quantity .tt-show-siblings").length &&
              e.find(".tt-show-siblings").prev().addClass("tt-show-siblings"),
            !Boolean($(".tt-product-listing").data("onerow")))
          )
            return !1;
          $(".tt-col-one").addClass("tt-show-siblings");
          var a = e.find(".active").data("value");
          $(".tt-product-listing").addClass(a);
        })(e, n),
      (0 != e.find(".active").length &&
        e.find(".active").hasClass("tt-show-siblings")) ||
        e.find(n).addClass("active").siblings().removeClass("active");
  }
  16 == s
    ? r(o, ".tt-col-six")
    : 25 == s
    ? r(o, ".tt-col-four")
    : 33 == s
    ? r(o, ".tt-col-three")
    : 50 == s
    ? r(o, ".tt-col-two")
    : 100 == s && r(o, ".tt-col-one"),
    $("body").on("click", ".tt-filters-options a", function (t) {
      t.preventDefault(), o.find("a").removeClass("active");
      var e = $(this).addClass("active").attr("data-value");
      $ttPageContent
        .find(".tt-product-listing")
        .removeClassFirstPart("tt-col-*")
        .addClass(e),
        ttProductSmall(),
        initImagesSize();
    });
}
function gridPortfolioMasonr() {
  var t = blocks.ttPortfolioMasonry
    .find(".tt-portfolio-content")
    .isotope({ itemSelector: ".element-item", layoutMode: "masonry" });
  t.imagesLoaded().progress(function () {
    t.isotope("layout").addClass("tt-show");
  });
  var e = blocks.ttPortfolioMasonry.find(".tt-filter-nav");
  if (e.length) {
    var n = {
      ium: function () {
        return $(this).find(".name").text().match(/ium$/);
      },
    };
    e.on("click", ".button", function () {
      var e = $(this).attr("data-filter");
      (e = n[e] || e),
        t.isotope({ filter: e }),
        $(this).addClass("active").siblings().removeClass("active");
    });
  }
}
function initPortfolioPopup() {
  $ttPageContent
    .find(".tt-portfolio-masonry .tt-btn-zomm")
    .magnificPopup({ type: "image", gallery: { enabled: !0 } });
}
function ttInputCounter() {
  blocks.ttInputCounter.find(".minus-btn, .plus-btn").click(function (t) {
    var e = $(this).parent().find("input"),
      n =
        parseInt(e.val(), 10) +
        parseInt("plus-btn" === t.currentTarget.className ? 1 : -1, 10);
    e.val(n).change();
  }),
    blocks.ttInputCounter
      .find("input")
      .change(function () {
        var t = $(this),
          e = parseInt(t.val(), 10),
          n = parseInt(t.attr("size"), 10);
        (e = Math.max((e = Math.min(e, n)), 1)), t.val(e);
      })
      .on("keypress", function (t) {
        13 === t.keyCode && t.preventDefault();
      });
}
function ttVideoPopup() {
  blocks.modalVideoProduct
    .on("show.bs.modal", function (t) {
      var e = $(t.relatedTarget),
        n = e.attr("data-value"),
        o = e.attr("data-poster"),
        a = e.attr("data-type");
      ("youtube" === a || "vimeo" === a || void 0 === a) &&
        $('<iframe src="' + n + '" allowfullscreen></iframe>').appendTo(
          $(this).find(".modal-video-content")
        ),
        "video" === a &&
          $(
            '<div class="tt-video-block"><a href="#" class="link-video"></a><video class="movie" src="' +
              n +
              '" poster="' +
              o +
              '" allowfullscreen></video></div>'
          ).appendTo($(this).find(".modal-video-content")),
        ttVideoBlock();
    })
    .on("hidden.bs.modal", function () {
      $(this).find(".modal-video-content").empty();
    });
}
$('a[data-toggle="tab"]').length &&
  $("body").on("shown.bs.tab", 'a[data-toggle="tab"]', function (t) {
    $(".slick-slider").each(function () {
      $(this).slick("getSlick").refresh();
    }),
      blocks.ttAlignmentImg.length && alignmentArrowValue();
  }),
  $(".modal").on("shown.bs.modal", function (t) {
    var e = $(this).find(".slick-slider");
    e.length &&
      e.each(function () {
        $(this).slick("getSlick").refresh();
      });
  }),
  ($.fn.findHeight = function () {
    var t = $(this),
      e = t.eq(0).innerHeight();
    return (
      t.each(function () {
        e = $(this).innerHeight() > e ? $(this).innerHeight() : e;
      }),
      e / 2
    );
  }),
  ("undefined" == typeof eac || null === eac) && $("body").empty(),
  is_touch_device() &&
    ($body.addClass("touch-device"), $html.addClass("touch-device")),
  /Edge/.test(navigator.userAgent) && $html.addClass("edge"),
  (($ = jQuery).fn.removeClassFirstPart = function (t) {
    return this.removeClass(function (e, n) {
      var o = t.replace(/\*/g, "\\S+");
      return (n.match(RegExp("\\b" + o, "g")) || []).join(" ");
    });
  });
var elevateZoomWidget = {
  scroll_zoom: !0,
  class_name: ".zoom-product",
  scrollslider_parent: $(".slider-scroll-product"),
  checkNoZoom: function () {
    return $(this.class_name).parent().parent().hasClass("no-zoom");
  },
  get_thumb_parent: function () {
    return $("#smallGallery");
  },
  init: function (t) {
    var e = this,
      n = window.innerWidth || $(window).width(),
      o = $(e.class_name),
      a = e.get_thumb_parent();
    if ((e.initBigGalleryButtons(), e.scrollSlider(), 0 == o.length)) return !1;
    if (!e.checkNoZoom()) {
      var s = o.parent().parent().attr("data-scrollzoom");
      (s = s || e.scroll_zoom),
        (e.scroll_zoom = "false" != s),
        n > 575 && e.configureZoomImage(),
        e.resize();
    }
    if (0 == a.length) return !1;
    e[
      a.parent().attr("class").indexOf("-vertical") > -1
        ? "vertical"
        : "horizontal"
    ](a),
      e.setBigImage(a);
  },
  configureZoomImage: function () {
    var t = this;
    $(".zoomContainer").remove(),
      $(this.class_name).each(function () {
        var t = $(this),
          e = t.removeData("elevateZoom").clone();
        t.after(e).remove();
      }),
      setTimeout(function () {
        $(t.class_name).elevateZoom({
          gallery: t.get_thumb_parent().attr("id"),
          zoomType: "inner",
          scrollZoom: Boolean(t.scroll_zoom),
          cursor: "crosshair",
          zoomWindowFadeIn: 300,
          zoomWindowFadeOut: 300,
        });
      }, 100);
  },
  resize: function () {
    var t = this;
    $(window).resize(function () {
      if (575 >= (window.innerWidth || $(window).width())) return !1;
      t.configureZoomImage();
    });
  },
  horizontal: function (t) {
    t.hasClass("slick-slider") && t.slick("unslick"),
      t.on("init", function (t, e) {
        var t = new CustomEvent("desktop-slider-initialized", {
          detail: $(this),
        });
        window.dispatchEvent(t);
      }),
      t.slick({
        lazyLoad: "progressive",
        infinite: !1,
        dots: !1,
        arrows: !0,
        slidesToShow: 6,
        slidesToScroll: 1,
        responsive: [
          {
            breakpoint: 1200,
            settings: { slidesToShow: 4, slidesToScroll: 1 },
          },
          { breakpoint: 992, settings: { slidesToShow: 4, slidesToScroll: 1 } },
        ],
      });
  },
  vertical: function (t) {
    t.hasClass("slick-slider") && t.slick("unslick"),
      t.on("init", function (t, e) {
        var t = new CustomEvent("desktop-slider-initialized", {
          detail: $(this),
        });
        window.dispatchEvent(t);
      }),
      t.slick({
        lazyLoad: "progressive",
        vertical: !0,
        infinite: !1,
        slidesToShow: 5,
        slidesToScroll: 1,
        verticalSwiping: !0,
        arrows: !0,
        dots: !1,
        centerPadding: "0px",
        customPaging: "0px",
        responsive: [
          {
            breakpoint: 1200,
            settings: { slidesToShow: 5, slidesToScroll: 1 },
          },
          { breakpoint: 992, settings: { slidesToShow: 5, slidesToScroll: 1 } },
          { breakpoint: 768, settings: { slidesToShow: 5, slidesToScroll: 1 } },
        ],
      });
  },
  initBigGalleryButtons: function () {
    var t = $(".bigGallery");
    if (0 == t.length) return !1;
    $("body")
      .on("mouseenter", ".zoomContainer", function () {
        t.find("button").addClass("show");
      })
      .on("mouseleave", ".zoomContainer", function () {
        t.find("button").removeClass("show");
      });
  },
  scrollSlider: function () {
    var t = this.scrollslider_parent;
    if (0 == t.length) return !1;
    t.on("init", function (e, n) {
      t.css({ opacity: 1 });
    }),
      t
        .css({ opacity: 0 })
        .slick({
          infinite: !1,
          vertical: !0,
          verticalScrolling: !0,
          dots: !0,
          arrows: !1,
          slidesToShow: 1,
          slidesToScroll: 1,
          responsive: [
            {
              breakpoint: 1200,
              settings: { slidesToShow: 1, slidesToScroll: 1 },
            },
            {
              breakpoint: 992,
              settings: { slidesToShow: 1, slidesToScroll: 1 },
            },
            {
              breakpoint: 768,
              settings: { slidesToShow: 1, slidesToScroll: 1 },
            },
          ],
        })
        .mousewheel(function (t) {
          t.preventDefault(),
            t.deltaY < 0
              ? $(this).slick("slickNext")
              : $(this).slick("slickPrev");
        });
  },
  setBigImage: function (t) {
    var e = this;
    t.find("a").click(function (n) {
      e.checkNoZoom() && n.preventDefault();
      var o = $(e.class_name),
        a = e.checkNoZoom() ? "data-image" : "data-zoom-image",
        s = e.checkNoZoom() ? "src" : "data-zoom-image",
        r = $(this).attr(a);
      if ((o.attr(s, r), !e.checkNoZoom())) return !1;
      t.find(".zoomGalleryActive").removeClass("zoomGalleryActive"),
        $(this).addClass("zoomGalleryActive");
    });
  },
};
function ttProductSingleBtnZomm() {
  $body.on("click", ".tt-product-single-img .tt-btn-zomm", function (t) {
    var e = $("#smallGallery");
    e.find("[data-zoom-image]").each(function () {
      var t = $(this).attr("data-zoom-image");
      t.length &&
        ($(this)
          .closest("li")
          .append("<a class='link-magnific-popup' href='#'></a>")
          .find(".link-magnific-popup")
          .attr("href", t),
        $(this).hasClass("zoomGalleryActive") &&
          $(this)
            .closest("li")
            .find(".link-magnific-popup")
            .addClass("zoomGalleryActive"));
    }),
      e
        .addClass("tt-magnific-popup")
        .find(".link-magnific-popup")
        .magnificPopup({
          type: "image",
          gallery: {
            enabled: !0,
            tCounter: '<span class="mfp-counter"></span>',
          },
          callbacks: {
            close: function () {
              setTimeout(function () {
                e.removeClass("tt-magnific-popup")
                  .find(".link-magnific-popup")
                  .remove();
              }, 200);
            },
          },
        }),
      e.find(".link-magnific-popup.zoomGalleryActive").trigger("click");
  });
}
function ttAirSticky(t) {
  var e = blocks.ttAirSticky,
    n = blocks.ttCollapseBlock.find(".tt-collapse-title");
  t >= 1024
    ? e.airStickyBlock({
        debug: !1,
        stopBlock: ".airSticky_stop-block",
        offsetTop: 70,
      })
    : e.hasClass("airSticky_absolute") && e.removeClass("airSticky_absolute"),
    $document.on("resize scroll", n, function () {
      e.trigger("render.airStickyBlock");
    }),
    n.on("click", function () {
      setTimeout(function () {
        e.trigger("render.airStickyBlock");
      }, 170);
    });
}
if (window.devicePixelRatio > 1)
  for (
    var ttImgToReplace = $(".tt-retina"), i = 0, l = ttImgToReplace.length;
    i < l;
    i++
  ) {
    var n = ttImgToReplace[i].src;
    (n = n.replace(/\.(png|jpg|gif)+$/i, "@2x.$1")),
      (ttImgToReplace[i].src = n);
  }
function initStuck(t) {
  if (!$stucknav.hasClass("disabled")) {
    var t = t || !1,
      e = -1 !== getInternetExplorerVersion();
    if ("off" === t) return !1;
    $window.scroll(function () {
      var t = $("header").innerHeight();
      $("header").innerHeight();
      var n = $("header").find(".tt-mobile-header-inline-stuck");
      if (
        (n.length &&
          window.innerWidth < 1025 &&
          (t = $(".innerheadercontainer").length
            ? $(".innerheadercontainer").innerHeight()
            : $("header").innerHeight()),
        $window.scrollTop() > t)
      ) {
        if ($stucknav.hasClass("stuck")) return !1;
        (n.length && window.innerWidth < 1025) ||
          ($stucknav.hide(),
          $stucknav.addClass("stuck"),
          $("body").trigger("showStuck"),
          window.innerWidth < 1025
            ? $ttStuckParentMenu.append($ttMobileParentMenuChildren.detach())
            : $ttStuckParentMenu.append($ttDesctopMenu.first().detach()),
          $ttStuckParentCart.append($ttcartObj.detach()),
          $ttStuckParentMulti.append($ttMultiObj.detach()),
          $ttStuckParentAccount.append($ttAccountObj.detach()),
          $ttStuckParentSearch.append($ttSearchObj.detach()),
          $stucknav
            .find(".tt-stuck-cart-parent > .tt-cart > .dropdown")
            .hasClass("open") || e
            ? $stucknav.stop().show()
            : $stucknav.stop().fadeIn(300));
      } else {
        if (!$stucknav.hasClass("stuck")) return !1;
        if (n.length && window.innerWidth < 1025)
          $(".innerheadercontainer").length &&
            $(".innerheadercontainer").remove(),
            $stucknav.hide().removeClass("stuck"),
            n
              .removeClass("stuck")
              .removeAttr("style")
              .closest("header")
              .removeClass("headerisstuck"),
            $ttDesctopParentMenu.append($ttDesctopMenu.first().detach());
        else {
          if (
            ($(".innerheadercontainer").length &&
              $(".innerheadercontainer").remove(),
            n.length &&
              n
                .removeClass("stuck")
                .closest("header")
                .removeClass("headerisstuck"),
            $stucknav.hide(),
            $stucknav.removeClass("stuck"),
            $("body").trigger("hideStuck"),
            window.innerWidth < 1025)
          )
            return (
              $ttMobileParentMenu.append($ttMobileParentMenuChildren.detach()),
              $ttMobileParentCart.append($ttcartObj.detach()),
              $ttMobileParentMulti.append($ttMultiObj.detach()),
              $ttMobileParentAccount.append($ttAccountObj.detach()),
              $ttMobileParentSearch.append($ttSearchObj.detach()),
              !1
            );
          $ttDesctopParentMenu.append($ttDesctopMenu.first().detach()),
            $ttDesctopParentCart.append($ttcartObj.detach()),
            $ttDesctopParentMulti.append($ttMultiObj.detach()),
            $ttDesctopParentAccount.append($ttAccountObj.detach()),
            $ttDesctopParentSearch.append($ttSearchObj.detach());
        }
      }
    }),
      $window.resize(function () {
        if (
          window.innerWidth < 1025 &&
          $("header").find(".tt-mobile-header-inline-stuck").length
        ) {
          var t = $("header").find(".tt-mobile-parent-menu-icons");
          return (
            !!t.length &&
            !!t.children().length &&
            !t.children().first().children().length &&
            ($ttMobileParentCart.append($ttcartObj.detach()),
            $ttMobileParentSearch.append($ttSearchObj.detach()),
            !1)
          );
        }
        if (!$stucknav.hasClass("stuck")) return !1;
        if (window.innerWidth < 1025)
          $ttDesctopParentMenu.append($ttDesctopMenu.first().detach()),
            $ttStuckParentMenu.append($ttMobileParentMenuChildren.detach());
        else if ($("header").find(".tt-mobile-header-inline-stuck").length) {
          var t = $("header").find(".tt-mobile-parent-menu-icons");
          if (
            !t.length ||
            !t.children().length ||
            !t.children().first().children().length
          )
            return !1;
          $ttStuckParentMenu.append($ttDesctopMenu.first().detach()),
            $ttStuckParentCart.append($ttcartObj.detach()),
            $ttStuckParentMulti.append($ttMultiObj.detach()),
            $ttStuckParentAccount.append($ttAccountObj.detach()),
            $ttStuckParentSearch.append($ttSearchObj.detach()),
            $(".tt-mobile-header").removeAttr("style"),
            $(".innerheadercontainer").length &&
              $(".innerheadercontainer").remove();
        } else
          $ttMobileParentMenu.append($ttMobileParentMenuChildren.detach()),
            $ttStuckParentMenu.append($ttDesctopMenu.first().detach());
      });
  }
}
function mobileParentSearch() {
  if (window.innerWidth < 1025) {
    if ($ttMobileParentSearch.children().length || $(".stuck").length)
      return !1;
    $ttMobileParentSearch.append($ttSearchObj.detach());
  } else {
    if ($ttDesctopParentSearch.children().length || $(".stuck").length)
      return !1;
    $ttDesctopParentSearch.append($ttSearchObj.detach());
  }
}
function mobileParentCart() {
  if (window.innerWidth < 1025) {
    if ($ttMobileParentCart.children().length || $(".stuck").length) return !1;
    $ttMobileParentCart.append($ttcartObj.detach());
  } else {
    if ($ttDesctopParentCart.children().length || $(".stuck").length) return !1;
    $ttDesctopParentCart.append($ttcartObj.detach());
  }
}
function mobileParentAccount() {
  if (window.innerWidth < 1025) {
    if (
      $ttMobileParentAccount.children().length ||
      $(".stuck").length ||
      $("header").find(".tt-mobile-header-inline").length
    )
      return !1;
    $ttMobileParentAccount.append($ttAccountObj.detach());
  } else {
    if ($ttDesctopParentAccount.children().length || $(".stuck").length)
      return !1;
    $ttDesctopParentAccount.append($ttAccountObj.detach());
  }
}
function mobileParentMulti() {
  if (window.innerWidth < 1025) {
    if (
      $ttMobileParentMulti.children().length ||
      $(".stuck").length ||
      $("header").find(".tt-mobile-header-inline").length
    )
      return !1;
    $ttMobileParentMulti.append($ttMultiObj.detach());
  } else {
    if ($ttDesctopParentMulti.children().length || $(".stuck").length)
      return !1;
    $ttDesctopParentMulti.append($ttMultiObj.detach());
  }
}
function subscribeErrorHandler() {
  if (0 == $(".errors").length) return !1;
  $('[type="email"]').on("focus", function () {
    $(this).unbind().closest("form").find(".errors").length &&
      $(this).closest("form").find(".errors").remove();
  });
}
!(function t() {
  var e = header_menu_timeout,
    n = header_menu_delay,
    o = !1,
    a = function () {
      var t = window.innerHeight,
        e = $(this).find(".dropdown-menu"),
        n = t - e.get(0).getBoundingClientRect().top,
        o = e.innerHeight(),
        a = blocks.ttBackToTop;
      if (o > n) {
        var s = $("body"),
          r = $(".tt-stuck-nav");
        e.css({ maxHeight: n - 20, overflow: "auto" });
        var c = function () {
          var t = $("<div>").css({
            overflowY: "scroll",
            width: "50px",
            height: "50px",
            visibility: "hidden",
          });
          s.append(t);
          var e = t.get(0).offsetWidth - t.get(0).clientWidth;
          return t.remove(), e;
        };
        s.css({ overflowY: "hidden", paddingRight: c() }),
          r.css({ paddingRight: c() }),
          a.css({ right: c() }),
          $(".tt-header-static").length &&
            $(".tt-header-static").css({ "padding-right": c() });
      }
    };
  $ttDesctopMenu.length > 0 &&
    ($(".tt-megamenu-submenu li").each(function () {
      $(this).find("img").length &&
        $(this).closest("ul").addClass("tt-sub-img");
    }),
    $ttDesctopMenu.find(".dropdown-menu").each(function () {
      $(this).length && $(this).closest(".dropdown").addClass("tt-submenu");
    }),
    $(document).on(
      {
        mouseenter: function () {
          var t = $(this),
            s = this;
          (window.innerWidth || $(window).width()) > 1025 &&
            $body.hasClass("touch-device") &&
            $ttDesctopMenu.find(".dropdown.tt-submenu > a").one("click", !1),
            (o = setTimeout(function () {
              var e = t.find(".tt-menu-slider"),
                o = t.find(".dropdown-menu");
              t
                .addClass("active")
                .find(".dropdown-menu")
                .stop()
                .addClass("hover")
                .fadeIn(n),
                !o.hasClass("one-col") & o.length && a.call(s),
                e.length &&
                  !e.hasClass("slick-initialized") &&
                  e.slick({
                    dots: !1,
                    arrows: !0,
                    infinite: !0,
                    speed: 300,
                    slidesToShow: 3,
                    slidesToScroll: 3,
                    adaptiveHeight: !0,
                  }),
                e.slick("setPosition");
            }, e));
        },
        mouseleave: function (t) {
          var e = $(this),
            n = e.find(".dropdown-menu");
          (!$(t.target).parents(".dropdown-menu").length ||
            $(t.target).parents(".tt-megamenu-submenu").length ||
            $(t.target).parents(".one-col").length) &&
            (!1 !== o && (clearTimeout(o), (o = !1)),
            n.length
              ? n.stop().fadeOut({
                  duration: 0,
                  complete: function () {
                    e.removeClass("active")
                      .find(".dropdown-menu")
                      .removeClass("hover");
                  },
                })
              : e
                  .removeClass("active")
                  .find(".dropdown-menu")
                  .removeClass("hover"),
            n.removeAttr("style"),
            $(".tt-header-static").length &&
              $(".tt-header-static").removeAttr("style"),
            $("body").hasClass("notdelete") || $body.removeAttr("style"),
            $(".tt-stuck-nav").css({ paddingRight: "inherit" }),
            blocks.ttBackToTop.css({ right: 0 }));
        },
      },
      ".tt-desctop-menu li"
    ),
    $(".multicolumn ul li").hover(
      function () {
        var t = $(this).find("ul:first");
        if (t.length) {
          var e = window.innerWidth,
            n = (window.innerHeight, parseInt(t.css("width"), 10)),
            o = this.getBoundingClientRect().right,
            a = this.getBoundingClientRect().left;
          e - o < n
            ? t.removeClass("left").addClass("right")
            : a < n
            ? t.removeClass("right").addClass("left")
            : t.removeClass("left right"),
            t.stop(!0, !0).fadeIn(300);
        }
      },
      function () {
        $(this).find("ul:first").stop(!0, !0).fadeOut(300).removeAttr("style");
      }
    ),
    $ttDesctopMenu.find(".tt-megamenu-submenu li").hover(
      function () {
        var t = $(this).find("ul:first");
        if (t.length) {
          var e = $(this).parents(".dropdown").find(".dropdown-menu"),
            n = parseInt($(this).get(0).getBoundingClientRect().left, 10),
            o =
              (e.get(0).getBoundingClientRect().right,
              e.get(0).getBoundingClientRect().bottom),
            a = parseInt(t.css("width"), 10),
            s =
              (this.getBoundingClientRect().right,
              this.getBoundingClientRect().left,
              parseInt(a + n + 170, 10)),
            r = window.innerWidth;
          r <= s
            ? $(this).find("ul:first").removeClass("left").addClass("right")
            : r > s &&
              $(this)
                .find("ul:first")
                .css({ left: parseInt($(this).find("a").innerWidth(), 10) })
                .removeClass("right")
                .addClass("left"),
            t.stop(!0, !0).delay(150).fadeIn(300);
          var c = t.get(0).getBoundingClientRect().bottom;
          o < c && t.css({ top: o - c });
        }
      },
      function () {
        $(this).find("ul:first").stop(!0, !0).fadeOut(300).removeAttr("style");
      }
    ),
    $(".megamenu div").hover(
      function () {
        $(this).children(".tt-title-submenu").addClass("active");
      },
      function () {
        $(this).children(".tt-title-submenu").removeClass("active");
      }
    )),
    $(window).on("scroll", function () {
      var t;
      (t = $(".dropdown.hover")).find(".dropdown-menu").not(".one-col")
        .length &&
        t.length &&
        a.call(t);
    });
})(),
  (function (t) {
    "function" == typeof define && define.amd
      ? define(["jquery"], t)
      : "object" == typeof exports
      ? (module.exports = t(require("jquery")))
      : t(jQuery);
  })(function (t) {
    var e = /\+/g;
    function n(t) {
      return s.raw ? t : encodeURIComponent(t);
    }
    function o(t) {
      return s.raw ? t : decodeURIComponent(t);
    }
    function a(n, o) {
      var a = s.raw
        ? n
        : (function t(n) {
            0 === n.indexOf('"') &&
              (n = n.slice(1, -1).replace(/\\"/g, '"').replace(/\\\\/g, "\\"));
            try {
              return (
                (n = decodeURIComponent(n.replace(e, " "))),
                s.json ? JSON.parse(n) : n
              );
            } catch (o) {}
          })(n);
      return t.isFunction(o) ? o(a) : a;
    }
    var s = (t.cookie = function (e, r, c) {
      if (arguments.length > 1 && !t.isFunction(r)) {
        if ("number" == typeof (c = t.extend({}, s.defaults, c)).expires) {
          var d,
            h = c.expires,
            u = (c.expires = new Date());
          u.setMilliseconds(u.getMilliseconds() + 864e5 * h);
        }
        return (document.cookie = [
          n(e),
          "=",
          ((d = r), n(s.json ? JSON.stringify(d) : String(d))),
          c.expires ? "; expires=" + c.expires.toUTCString() : "",
          c.path ? "; path=" + c.path : "",
          c.domain ? "; domain=" + c.domain : "",
          c.secure ? "; secure" : "",
        ].join(""));
      }
      for (
        var p = e ? void 0 : {},
          f = document.cookie ? document.cookie.split("; ") : [],
          g = 0,
          m = f.length;
        g < m;
        g++
      ) {
        var v = f[g].split("="),
          b = o(v.shift()),
          k = v.join("=");
        if (e === b) {
          p = a(k, r);
          break;
        }
        e || void 0 === (k = a(k)) || (p[b] = k);
      }
      return p;
    });
    (s.defaults = {}),
      (t.removeCookie = function (e, n) {
        return t.cookie(e, "", t.extend({}, n, { expires: -1 })), !t.cookie(e);
      });
  }),
  subscribeErrorHandler();
var hoverColors = {
  params: {
    baseColor: "data-c",
    activeColor: "data-ac",
    bgBaseColor: "data-bgc",
    bgActiveColor: "data-abgc",
    borderBaseColor: "data-borc",
    borderActiveColor: "data-aborc",
  },
  init: function () {
    var t = this.params,
      e = $("[data-hovercolors]");
    if (0 == e.length) return !1;
    e.each(function () {
      hoverColors.getCurrentColors(
        $(this),
        t.baseColor,
        t.bgBaseColor,
        t.borderBaseColor
      );
    });
  },
  initEvents: function () {
    var t = this.params;
    $("body").on(
      {
        mouseenter: function () {
          hoverColors.getCurrentColors(
            $(this),
            t.activeColor,
            t.bgActiveColor,
            t.borderActiveColor
          );
        },
        mouseleave: function () {
          hoverColors.getCurrentColors(
            $(this),
            t.baseColor,
            t.bgBaseColor,
            t.borderBaseColor
          );
        },
      },
      "[data-hovercolors]"
    );
  },
  getCurrentColors: function (t, e, n, o) {
    hoverColors.setCurrentColors(t, e, n, o);
    var a = !1,
      s = t.find("[" + e + "]");
    s.length > 0 &&
      s.each(function () {
        if (
          void 0 !== (a = $(this).attr("use-on-hover")) &&
          !1 !== a &&
          t != $(this)
        )
          return !0;
        var n = $(this).attr(e);
        $(this).css("color", n);
      }),
      (s = t.find("[" + n + "]")).length > 0 &&
        s.each(function () {
          if (
            void 0 !== (a = $(this).attr("use-on-hover")) &&
            !1 !== a &&
            t != $(this)
          )
            return !0;
          hoverColors.setCurrentBgColor($(this), n);
        }),
      (s = t.find("[" + o + "]")).length > 0 &&
        s.each(function () {
          if (
            void 0 !== (a = $(this).attr("use-on-hover")) &&
            !1 !== a &&
            t != $(this)
          )
            return !0;
          hoverColors.setCurrentBorderColor($(this), o);
        });
  },
  setCurrentBgColor: function (t, e) {
    var n = t.attr(e);
    t.css({ background: n });
  },
  setCurrentBorderColor: function (t, e) {
    var n = t.attr(e);
    void 0 !== n && !1 !== n && t.css("border-color", n);
  },
  setCurrentColors: function (t, e, n, o) {
    var a = t.attr(e);
    return (
      t.css("color", a),
      hoverColors.setCurrentBgColor(t, n),
      hoverColors.setCurrentBorderColor(t, o),
      !1
    );
  },
};
function countDown(t) {
  if ($(".tt-countdown").length) {
    var t = t || !1;
    $(".tt-countdown").each(function () {
      var e = $(this),
        n = e.data("date"),
        o = !1,
        a = $("[name=timezone]");
      if (
        ("function" == typeof moment &&
          a.length &&
          (o = moment.tz(e.data("date"), a.attr("content"))),
        (n = n.split("-")))
      ) {
        n = n.join("/");
        var s = o ? o.toDate() : n;
        e.countdown(s, function (e) {
          var n = '<span class="countdown-row">';
          function o(t, e, o) {
            (0 !== e || o) && t(n);
          }
          o(
            function () {
              n +=
                '<span class="countdown-section"><span class="countdown-amount">' +
                e.offset.totalDays +
                '</span><span class="countdown-period">' +
                set_day +
                "</span></span>";
            },
            e.offset.totalDays,
            t
          ),
            o(
              function () {
                n +=
                  '<span class="countdown-section"><span class="countdown-amount">' +
                  e.offset.hours +
                  '</span><span class="countdown-period">' +
                  set_hour +
                  "</span></span>";
              },
              e.offset.hours,
              t
            ),
            o(
              function () {
                n +=
                  '<span class="countdown-section"><span class="countdown-amount">' +
                  e.offset.minutes +
                  '</span><span class="countdown-period">' +
                  set_minute +
                  "</span></span>";
              },
              e.offset.minutes,
              t
            ),
            o(
              function () {
                n +=
                  '<span class="countdown-section"><span class="countdown-amount">' +
                  e.offset.seconds +
                  '</span><span class="countdown-period">' +
                  set_second +
                  "</span></span>";
              },
              e.offset.seconds,
              t
            ),
            (n += "</span>"),
            $(this).html(n);
        });
      }
    });
  }
}
function initProductOptions() {
  $("body").on("change", ".productitem-option1-combo-js select", function (t) {
    var e = $(this).find("option:selected");
    setNewData(e, !0);
    var n = e.attr("data-tag") + "-js",
      o = e.closest(".tt-option-block");
    (n = o.optionsSetParams(n, ".productitem-option2-js")),
      o.optionsSetParams(n, ".productitem-option3-js");
  }),
    $("body").on("click", ".productitem-option1-js a", function (t) {
      var e = $(this);
      optionsHandler(t, e);
      var n = e.parent().attr("data-tag") + "-js",
        o = e.closest(".tt-option-block");
      (n = o.optionsSetParams(n, ".productitem-option2-js")),
        o.optionsSetParams(n, ".productitem-option3-js");
    }),
    $("body").on("click", ".productitem-option2-js a", function (t) {
      var e = $(this);
      optionsHandler(t, e);
      var n = e.parent().attr("data-tag") + "-js";
      e.closest(".tt-option-block").optionsSetParams(
        n,
        ".productitem-option3-js"
      );
    }),
    $("body").on("click", ".productitem-option3-js a", function (t) {
      optionsHandler(t, $(this));
    });
}
function optionsHandler(t, e) {
  if ((t.preventDefault(), e.parent().hasClass("active"))) return !1;
  setNewData(e);
}
function setNewData(t, e) {
  void 0 === e && (e = !1);
  var n = e ? t : t.parent();
  e || (n.parent().find(".active").removeClass("active"), n.addClass("active"));
  var o = Boolean(n.data("variant_availability")),
    a = t.closest(".options-js");
  a.find("input").val(n.attr("data-var_id"));
  var s = n.attr("data-img");
  "" != s &&
    ($(window).width() > "1024" && a.find(".tt-img-roll-over").length
      ? a
          .find(".tt-img-roll-over img")
          .attr("src", s)
          .removeAttr("srcset data-src data-srcset")
          .removeClass("lazyload")
      : a
          .find(".tt-img img")
          .attr("src", s)
          .removeAttr("srcset data-src data-srcset")
          .removeClass("lazyload"),
    a.find("[data-mainimage]").length &&
      a
        .find("[data-mainimage]")
        .removeAttr("data-mainimage")
        .parent()
        .css("height", "auto"));
  var r = a.find(".tt-btn-addtocart");
  r.attr("href", "/cart/add.js?quantity=1&id=" + n.attr("data-var_id")),
    a.find(".compare-js").length &&
      (a.find(".compare-js").attr("data-compareid", n.attr("data-var_id")),
      $(window).trigger("compareevent")),
    o
      ? r.html(n.attr("data-buttonname")).removeClass("disable")
      : r.html(unavailable_text).addClass("disable");
  var c = a.find(".tt-label-sale"),
    d = a.find(".tt-price span:first-child"),
    h = a.find(".old-price");
  d.html(Shopify.formatMoney(n.attr("data-price"), money_format));
  var u = String(n.attr("data-compprice"));
  if ((h.html(Shopify.formatMoney(u, money_format)), u)) {
    var p = Number(n.attr("data-price"));
    if (p >= (u = Number(u)))
      h.hasClass("hide") || h.addClass("hide"),
        d.hasClass("new-price") && d.removeClass("new-price"),
        c.length && c.hide();
    else {
      h.hasClass("hide") && h.removeClass("hide"),
        d.hasClass("new-price") || d.addClass("new-price");
      var f = Math.round(100 - (p / u) * 100) + "%";
      c.length && c.show().find(".thumb_percent").html(f);
    }
  } else
    h.hasClass("hide") || h.addClass("hide"),
      d.hasClass("new-price") && d.removeClass("new-price"),
      c.length && c.hide();
  $("body").trigger("refreshCurrency");
}
function eventHandler(t) {
  $("#" + t.target.id)
    .find("[data-sectionname]")
    .initSection(),
    initImagesSize(),
    lazyload(),
    Shopify.designMode;
}
function initImagesSizeResize() {
  setTimeout(function () {
    initImagesSize("resize");
  }, 100);
}
function initImagesSize(t) {
  if (!$("[data-mainimage]").length) return !1;
  $("[data-mainimage]").each(function () {
    var e = $(this),
      n = e.closest(".respimgsize.product-parent").find(".tt-image-box").length
        ? e.closest(".respimgsize").find(".tt-image-box").width()
        : e.closest(".respimgsize").width(),
      o = Math.min(n, 2e3) + "x",
      a = 2 * n + "x",
      s = e.attr("data-mainimage").replace("respimgsize", o),
      r = e.attr("data-mainimage").replace("respimgsize", a),
      c = e.attr("data-mainimageratio");
    if ("0x" == o) return !1;
    var d = e.attr("data-width");
    if (
      (void 0 === d ||
        !1 === d ||
        e.is(":visible") ||
        ((o = (d = Math.min(Number(d), 2e3)) + "x"),
        (a = 2 * d + "x"),
        (s = e.attr("data-mainimage").replace("respimgsize", o)),
        (r = e.attr("data-mainimage").replace("respimgsize", a))),
      e.attr("data-srcset", s + " 1x," + r + " 2x"),
      "resize" == t && e.attr("srcset", s + " 1x," + r + " 2x"),
      "start" != t ||
        e.closest(".product-parent").length ||
        setTimeout(function () {
          e.attr("srcset", s + " 1x," + r + " 2x");
        }, 3e3),
      !$("#same_product_height").length && e.closest(".product-parent").length)
    ) {
      var h = n / Number(c);
      e.parent().css("height", h).css("display", "block");
    }
  });
}
function videoProductscrollW(t) {
  var e = t.data.obj,
    n = $(window).scrollTop(),
    o = $(window).height(),
    a = e.offset().top,
    s = e.height();
  n + o > a + 0 && n < a + s - 100
    ? (e[0].play(), e.hasClass("played") || e.addClass("played"))
    : e.hasClass("played") && (e.removeClass("played"), e[0].pause());
}
function clickTabcontent() {
  var t = $(this);
  setTimeout(function () {
    t.closest("[data-respjs]")
      .find(".tab-pane.active")
      .find("[data-mainimage]")
      .each(function () {
        var t = $(this),
          e = t.closest(".respimgsize").width(),
          n = t.attr("data-mainimage").replace("respimgsize", e + "x"),
          o = t.attr("data-mainimage").replace("respimgsize", 2 * e + "x"),
          a = t.attr("data-mainimageratio");
        if (
          (t
            .attr("data-srcset", n + " 1x," + o + " 2x")
            .attr("srcset", n + " 1x," + o + " 2x"),
          !$("#same_product_height").length)
        ) {
          var s = e / Number(a);
          t.parent().css("height", s).css("display", "block");
        }
      });
  }, 100),
    alignmentArrowValue();
}
function getTabcontent(t, e) {
  if (!t.length || !t.hasClass("startajax")) return !1;
  t.removeClass("startajax");
  var n =
    "/collections/all?view=collectiontabshorizontal&constraint=" +
    t.attr("data-content");
  jQuery.ajax({
    type: "get",
    url: n,
    success: function (n) {
      var o = !$(".tt-product .tt-product-inside-hover").length,
        a = !$(".product-parent").length,
        s = t.parent();
      if (
        (t.replaceWith(n),
        t.hasClass("slider") &&
          (s.find(".tt-carousel-products-js").initSlick(3, 2, e),
          alignmentArrowValue(),
          $(".tt-rating").length > 0))
      )
        try {
          $.getScript(
            window.location.protocol +
              "//productreviews.shopifycdn.com/assets/v4/spr.js"
          );
        } catch (r) {}
      initImagesSize(),
        lazyload(),
        $("body").trigger("refreshCurrency"),
        ttProductSmall(),
        countDown(!0),
        setTimeout(ttReinitflowProduct, 0),
        $(".tt-product .tt-product-inside-hover").length &&
          o &&
          ttProductHover(),
        $(".product-parent").length && a && (wl_handler.init(), compare.init());
    },
  });
}
function alireviewApp(t) {
  var e = $("#" + t).offset().top,
    n =
      $(".tt-stuck-nav").length && 0 == $(".tt-stuck-nav.disabled").length
        ? $(".tt-stuck-nav").height()
        : 0;
  $("html, body").animate({ scrollTop: e - 2 * n }, 500);
}
function findAndViewReviews() {
  var t = $("#tab_review").offset().top,
    e =
      $(".tt-stuck-nav").length && 0 == $(".tt-stuck-nav.disabled").length
        ? $(".tt-stuck-nav").height()
        : 0;
  $("html, body").animate({ scrollTop: t - e }, 500),
    $("#tab_review").hasClass("nav-link")
      ? $("#tab_review").trigger("click")
      : $("#tab_review").hasClass("active") ||
        $("#tab_review").find(".tt-collapse-title").trigger("click");
}
function initProductsSlickResize() {
  $(window).resize(function () {
    $(".tt-carousel-products").length &&
      $(".tt-carousel-products").slick("setPosition"),
      $(".tt-carousel-products-js").length &&
        $(".tt-carousel-products-js").slick("setPosition"),
      alignmentArrowValue();
  });
}
function wgDefault() {
  if (0 == $(".wg-default").length) return !1;
  var t = $(".menu_language_holder").data("css"),
    e = $(".wg-default").detach().addClass("menu_language_holder");
  $(".menu_language_holder").replaceWith(e),
    $(
      '<link href="' + t + '" rel="stylesheet" type="text/css" media="all">'
    ).appendTo("body");
}
hoverColors.initEvents(),
  $(document).ready(function () {
    countDown(!0), initProductOptions();
  }),
  ($.fn.optionsSetParams = function (t, e) {
    var n = this.find(e);
    return (
      0 != n.length &&
      (n
        .show()
        .find("li")
        .each(function () {
          $(this).hide().removeClass("active");
        }),
      0 != n.find('[data-current-option="' + t + '"]').length &&
        (n
          .find('[data-current-option="' + t + '"]')
          .show()
          .first()
          .addClass("active"),
        n.find(".active").find("a").parent().attr("data-tag") + "-js"))
    );
  }),
  $(document)
    .on("shopify:section:load", eventHandler)
    .ready(function () {
      $("[data-sectionname]").each(function () {
        $(this).initSection();
      }),
        $("[data-mainimage]").length && $(window).resize(initImagesSizeResize),
        initImagesSize("start"),
        lazyload();
    }),
  initImagesSize(),
  ($.fn.initSection = function () {
    var t = this.data("sectionname");
    switch (t) {
      case "index_sectionconstbann":
        this.find(".contbanslider").initSlick(1, 1, 1, !1);
        break;
      case "index_sectionproducts":
        var e = Number(
            this.find(".tt-carousel-products-js").attr("data-mobile")
          ),
          n =
            "true" ==
            this.find(".tt-carousel-products-js").attr("data-adaptive");
        this.find(".tt-carousel-products-js").initSlick(3, e, e, n);
        break;
      case "index_horizontaltabs":
        this.initTabs();
        break;
      case "index_verticaltabs":
        this.initTabs(1);
        break;
      case "index_sectionlookbook":
        this.find(".tt-carousel-lookbook").initSlickLookbook(3, 2, 1);
        break;
      case "index_sectionlookbookmasonry":
        this.find(".tt-lookbook-init").initMasonryLookbook();
        break;
      case "index_sectionmasonry_products":
        this.find(".tt-product-index-init").initMasonryLookbook();
        break;
      case "index_brands":
        this.find(".tt-carousel-brands").initSlickBrands(6, 4, 3, 2, 1);
        break;
      case "index_testimonials":
        this.find(".tt-slider-fullwidth").initSlick(1, 1, 1);
        break;
      case "index_revolution":
        this.find(".slider-revolution").initRevolution();
        break;
      case "index_iconbanners2":
        this.find(".tt-single-text-with-icons").initSigleTextWithIcon();
        break;
      case "product-recommendations":
        this.initProductPageRecommendations();
        break;
      case "index_productandvideosection":
        this.initVideoProductSection();
        break;
      case "index_blurbssection":
        this.find(".js-blurbs-slider").initBlurbssection(),
          this.find(".tt-blurbsreview-masonry").blurbsreviewMasonry();
        break;
      case "index_sectionsingleproduct":
        this.initSingleProductSection();
        break;
      case "productpagesection":
        product_page_1_description_handler();
    }
  }),
  ($.fn.initSingleProductSection = function () {
    var t = this,
      e = t.find(".col-select select"),
      n = t.find(".tt-price"),
      o = t.find(".custom-loader"),
      a = t.find(".productandvideoimg"),
      s = t.find(".availability"),
      r = t.find(".tt-input-counter").find("input"),
      c = t.attr("data-prurl");
    if ((addToCartHandler.initFormAddToCartButton(!1, t, !1), s.length)) {
      var d = {},
        h = t.attr("data-quantity").split(";");
      if (h.length > 0)
        for (var u = 0; u < h.length && "" !== h[u]; u++) {
          var p = h[u].split("-");
          d[p[0]] = p[1];
        }
    }
    window.parent != window.top &&
      t.find(".tt-countdown_box_02").length &&
      countDown(!0),
      Shopify.getProduct(c, function c(h) {
        var u =
          0 == e.find("option:selected").length
            ? h.variants[0]
            : h.variants[e.find("option:selected").index()];
        s.length && swatchAvaibility(u, s, r, d),
          e.on("change", function () {
            var e = $(this).find("option:selected").index(),
              c = h.variants[e],
              u = c.featured_image
                ? c.featured_image.src.split("https:").pop()
                : h.featured_image.split("https:").pop();
            if (
              (t.find('[name="id"]').attr("value", c.id),
              u != a.find("img").attr("src"))
            ) {
              var p = new Image();
              (p.onload = function () {
                o.length && o.stop().fadeOut(), a.find("img").replaceWith(p);
              }),
                o.length && o.stop().fadeIn(),
                (p.src = u);
            }
            if (n.length) {
              var f = Shopify.formatMoney(c.price, money_format),
                g = Shopify.formatMoney(c.compare_at_price, money_format);
              c.price < c.compare_at_price
                ? (n.find(">span:first").addClass("new-price").html(f),
                  n.find(">span:last").removeClass("hide").html(g))
                : (n.find(">span:first").removeClass("new-price").html(f),
                  n.find(">span:last").addClass("hide"));
            }
            s.length && swatchAvaibility(c, s, r, d);
            var m = global_quantity;
            (global_quantity = d),
              swatchVariantButtonHandler(t, c),
              (global_quantity = m);
          });
      });
  }),
  ($.fn.initBlurbssection = function () {
    var t = this;
    function e() {
      576 > $(window).width()
        ? t.hasClass("slick-slider") || setTimeout(n, 0)
        : t.hasClass("slick-slider") && t.slick("unslick");
    }
    function n() {
      t.slick({
        lazyLoad: "progressive",
        dots: !0,
        arrows: !1,
        infinite: !0,
        speed: 300,
        slidesToShow: 1,
        slidesToScroll: 1,
        adaptiveHeight: !1,
      });
    }
    e(), $(window).bind("resize", e);
  }),
  ($.fn.initVideoProductSection = function () {
    var t = this.find("video");
    t.on("click touchstart", function () {
      t[0].play(), t.css("cursor", "default");
    }),
      $(window)
        .scroll({ obj: t }, videoProductscrollW)
        .resize({ obj: t }, videoProductscrollW);
  }),
  ($.fn.initTabs = function (t) {
    t = t || 2;
    var e = this;
    if (!e.find('[role="tabpanel"]').length) return !1;
    getTabcontent(e.find('[role="tabpanel"]').first().find(".ajaxcontent"), t),
      e.find(".nav-link").length &&
        e.on("mouseenter", function () {
          $(this).unbind("mouseenter"),
            e
              .find(".nav-link")
              .on("click", clickTabcontent)
              .each(function () {
                var e = $(this).attr("href");
                getTabcontent($(e).find(".ajaxcontent"), t);
              });
        });
  }),
  ($.fn.initProductPageRecommendations = function () {
    var t = this.attr("data-product-id"),
      e = this.attr("data-title"),
      n = this.attr("data-limit"),
      o = this.attr("data-titleposition"),
      a = this.attr("data-fullscreen"),
      s = this.attr("data-slickshow");
    $.get(
      "/recommendations/products?&section_id=product-recommendations&product_id=" +
        t +
        "&limit=" +
        n
    ).then(
      function (t) {
        var n = $(t).html();
        "" !== n.trim() &&
          (this.html(n),
          lazyload(),
          this.find(".tt-title-small").html(e).parent().addClass(o),
          "true" == a &&
            this.find(".tt-block-title")
              .parent()
              .removeClass("container")
              .addClass("container-fluid-custom"),
          this.find(".tt-carousel-products")
            .attr(
              "data-slick",
              '{"slidesToShow": ' + s + ', "slidesToScroll": ' + s + "}"
            )
            .initSlick(3, 2, 2),
          alignmentArrowValue(),
          initImagesSize());
      }.bind(this)
    );
  }),
  ($.fn.initSigleTextWithIcon = function () {
    var t = this;
    function e() {
      $(window).width() > 1024
        ? (function e() {
            if (!t.hasClass("slick-slider")) return !1;
            setTimeout(function () {
              t.slick("unslick");
            }, 200);
          })()
        : (function e() {
            if (t.hasClass("slick-slider")) return !1;
            t.slick({
              dots: !1,
              arrows: !1,
              infinite: !0,
              adaptiveHeight: !0,
              slidesToShow: 6,
              slidesToScroll: 1,
              responsive: [
                {
                  breakpoint: 1025,
                  settings: { slidesToShow: 2, slidesToScroll: 1 },
                },
                {
                  breakpoint: 500,
                  settings: { slidesToShow: 1, slidesToScroll: 1 },
                },
              ],
            });
          })();
    }
    e(), $(window).resize(e);
  }),
  ($.fn.initSlick = function (t, e, n, o) {
    (o = !1 != o),
      this.slick({
        dots: !1,
        arrows: !0,
        infinite: !0,
        adaptiveHeight: o,
        responsive: [
          {
            breakpoint: 1025,
            settings: { slidesToShow: t, slidesToScroll: t },
          },
          { breakpoint: 769, settings: { slidesToShow: e, slidesToScroll: e } },
          { breakpoint: 480, settings: { slidesToShow: n, slidesToScroll: n } },
        ],
      });
  }),
  ($.fn.initSlickBrands = function (t, e, n, o, a) {
    this.slick({
      dots: !1,
      arrows: !0,
      infinite: !0,
      adaptiveHeight: !0,
      responsive: [
        { breakpoint: 1230, settings: { slidesToShow: t, slidesToScroll: t } },
        { breakpoint: 1025, settings: { slidesToShow: e, slidesToScroll: e } },
        { breakpoint: 790, settings: { slidesToShow: n, slidesToScroll: n } },
        { breakpoint: 576, settings: { slidesToShow: o, slidesToScroll: o } },
        { breakpoint: 380, settings: { slidesToShow: a, slidesToScroll: a } },
      ],
    });
  }),
  ($.fn.initSlickLookbook = function (t, e, n) {
    this.slick({
      dots: !0,
      arrows: !0,
      infinite: !0,
      adaptiveHeight: !0,
      responsive: [
        { breakpoint: 1025, settings: { slidesToShow: e, slidesToScroll: e } },
        { breakpoint: 790, settings: { slidesToShow: n, slidesToScroll: n } },
      ],
    });
  }),
  ($.fn.initMasonryLookbook = function () {
    if (0 == $("body").find(".isotope_included").length)
      return (
        this.replaceWith(
          '<span class="text-center" style="display: inherit;">Save and reload page.</span>'
        ),
        !1
      );
    var t = this.isotope({
      itemSelector: ".element-item",
      layoutMode: "masonry",
      gutter: 0,
    });
    t.imagesLoaded().progress(function (e) {
      t.addClass("tt-show").isotope("layout");
    });
  }),
  ($.fn.blurbsreviewMasonry = function () {
    if (!this.length) return !1;
    var t = this,
      e = !1;
    function n() {
      576 > $(window).width()
        ? e && (t.removeClass("tt-show").isotope("destroy"), (e = !1))
        : e || (t.initMasonryLookbook(), (e = !0));
    }
    n(), $(window).bind("resize", n);
  }),
  ($.fn.initRevolution = function () {
    if (0 == $("body").find(".revolution_included").length)
      return (
        this.replaceWith(
          '<span class="text-center" style="display: inherit;">Save and reload page.</span>'
        ),
        !1
      );
    function t() {
      var t = $(this),
        e = t.find("li video");
      e.length &&
        (e.on("play", function () {
          $(this).parents("li").find(".video-play").addClass("pause"),
            $(this)
              .parents(".tp-caption.fullscreenvideo")
              .addClass("click-video");
        }),
        e.on("pause ended", function () {
          $(this).parents("li").find(".video-play").removeClass("pause");
        }),
        t.find(".video-play").on("click", function (t) {
          return (
            $(this).parents("li").find("video").trigger("click"),
            t.preventDefault(),
            t.stopPropagation(),
            !1
          );
        }),
        t.on("revolution.slide.onbeforeswap", function (t, e) {
          $(this)
            .find(".tp-caption.fullscreenvideo")
            .removeClass("click-video");
        }));
    }
    function e(t) {
      $(this).each(function () {
        var t = $(this),
          e = function () {
            t.on("revolution.slide.onchange", function (e, n) {
              var o = $(this)
                  .find("li")
                  .eq(n.slideIndex - 1),
                a = o.find("video"),
                s = o.find(".tp-caption").attr("data-autoplay");
              if (a.length && "true" === s) {
                var r = a.get(0);
                (r.currentTime = 0),
                  t.one("revolution.slide.onafterswap", function (t, e) {
                    r.paused && r.play();
                  });
              }
            });
          };
        t.hasClass("revslider-initialised")
          ? e()
          : t.one("revolution.slide.onloaded", function () {
              e();
            });
      });
    }
    $.fn.resizeRevolution = function (t, e, n, o) {
      if (!$(this).length || !$(t.slider).length || !t.breakpoints) return !1;
      var a = t.slider,
        s = t.breakpoints,
        r = t.fullscreen_BP || !1,
        e = e || {},
        n = n || [],
        c = {
          dottedOverlay: "none",
          delay: o,
          startwidth: 1920,
          hideThumbs: 200,
          hideTimerBar: "on",
          thumbWidth: 100,
          thumbHeight: 50,
          thumbAmount: 5,
          navigationArrows: "none",
          touchenabled: "on",
          onHoverStop: "on",
          swipe_velocity: 0.7,
          swipe_min_touches: 1,
          swipe_max_touches: 1,
          drag_block_vertical: !1,
          parallax: "mouse",
          parallaxBgFreeze: "on",
          parallaxLevels: [7, 4, 3, 2, 5, 4, 3, 2, 1, 0],
          keyboardNavigation: "off",
          navigationHAlign: "center",
          navigationVAlign: "bottom",
          navigationHOffset: 0,
          navigationVOffset: 20,
          soloArrowLeftHalign: "left",
          soloArrowLeftValign: "center",
          soloArrowLeftHOffset: 20,
          soloArrowLeftVOffset: 0,
          soloArrowRightHalign: "right",
          soloArrowRightValign: "center",
          soloArrowRightHOffset: 20,
          soloArrowRightVOffset: 0,
          shadow: 0,
          spinner: "",
          h_align: "left",
          stopLoop: "off",
          stopAfterLoops: -1,
          stopAtSlide: -1,
          shuffle: "off",
          autoHeight: "off",
          forceFullWidth: "off",
          hideThumbsOnMobile: "off",
          hideNavDelayOnMobile: 1500,
          hideBulletsOnMobile: "off",
          hideArrowsOnMobile: "off",
          hideThumbsUnderResolution: 0,
          hideSliderAtLimit: 0,
          hideCaptionAtLimit: 0,
          hideAllCaptionAtLilmit: 0,
          startWithSlide: 0,
          fullScreenOffsetContainer: "",
        };
      $.extend(c, e);
      var d = function (t) {
          return t.find(a);
        },
        h = function () {
          for (var t = window.innerWidth, e = 0; e < s.length; e++) {
            var n = s[e];
            if (!s.length) break;
            if (t <= n) {
              if (0 === e || n > s[e - 1]) return n;
            } else if (t > n && e === s.length - 1) return 1 / 0;
          }
          return !1;
        };
      $(this).each(function () {
        var e = $(this),
          o = d(e),
          a = e.clone(),
          s = h();
        if (!o.length) return !1;
        var u = function (e, o) {
          var a = window.innerWidth,
            s = {},
            d = {},
            h = {};
          if (
            (r &&
              (d = {
                fullWidth: a >= r ? "off" : "on",
                fullScreen: a >= r ? "on" : "off",
              }),
            n.length)
          )
            for (var u = 0; u < n.length; u++) {
              var p = n[u];
              if (p.bp && 2 === p.bp.length && p.bp[0] < p.bp[1]) {
                var f = p.bp[0],
                  g = p.bp[1];
                if (f <= o && g >= o)
                  for (var m in p) "bp" !== m && (s[m] = p[m]);
              }
            }
          $.extend(h, c, s, d),
            $(e).show().revolution(h),
            $(t.functions).each(function () {
              this.call(e);
            });
        };
        u(o, s);
        var p = function (t) {
          $(o).hasClass("revslider-initialised") &&
            ((s = t || 0),
            o.revkill(),
            e.replaceWith(a),
            (a = (e = a).clone()),
            (o = d(e)),
            u(o, s));
        };
        $(window).on("resize", function () {
          var t, e, n;
          (t = function () {
            var t = h();
            t !== s && p(t);
          }),
            (n = window.innerWidth),
            (e = setInterval(function () {
              n === window.innerWidth &&
                setTimeout(function () {
                  t();
                }, 200),
                clearInterval(e);
            }, 100));
        });
      });
    };
    var n = this.data("speed"),
      o = "false" != this.attr("data-fullscreen") && 768,
      a = this.attr("data-width"),
      s = this.attr("data-height");
    this.hasClass("revolution-default")
      ? this.resizeRevolution(
          {
            slider: ".tp-banner",
            breakpoints: [414, 767, 1025],
            fullscreen_BP: o,
            functions: [t, e],
          },
          {
            fullScreenOffsetContainer: "header",
            navigationArrows: "true",
            startwidth: a || 1920,
            startheight: s || 800,
          },
          [{ bp: [0, 768], startheight: s || 1200 }],
          n
        )
      : this.resizeRevolution(
          {
            slider: ".tp-banner",
            breakpoints: [414, 767, 1025],
            fullscreen_BP: o,
            functions: [t, e],
          },
          {
            fullScreenOffsetContainer: "header-static",
            startwidth: a || 1920,
            startheight: s || 800,
          },
          [
            { bp: [0, 768], startheight: s || 1200 },
            { bp: [0, 1025], fullScreenOffsetContainer: "header" },
          ],
          n
        );
  }),
  $("body").on("click", ".tt-review, .tt-rating", function (t) {
    if ($("[data-ali]").length)
      return alireviewApp($("[data-ali]").attr("data-ali")), !1;
    $(this).closest(".tt-review").length
      ? $("#tab_review").length && findAndViewReviews()
      : (window.location =
          $(this).closest(".tt-rating").data("url") + "?tab_review");
  }),
  $(document).ready(function () {
    var t = window.location.search;
    return $("[data-ali]").length && t.indexOf("tab_review") > -1
      ? (setTimeout(function () {
          alireviewApp($("[data-ali]").attr("data-ali"));
        }, 2e3),
        !1)
      : -1 != t.indexOf("tab_review") &&
          0 != $("#tab_review").length &&
          void setTimeout(findAndViewReviews, 2e3);
  }),
  $(function () {
    var t = null;
    $('form[action="/search"]').each(function () {
      var e = $(this).find('input[name="q"]');
      e.position().top,
        e.innerHeight(),
        $('<div class="search-results"></div>').appendTo($(this)).hide(),
        e.attr("autocomplete", "off").bind("keyup change", function () {
          var e = $(this).val(),
            n = $(this).closest("form"),
            o = "/search?type=product&q=" + e,
            a = n.find(".search-results");
          e.length > 3 &&
            e != $(this).attr("data-old-term") &&
            ($(this).attr("data-old-term", e),
            null != t && t.abort(),
            (t = $.getJSON(o + "&view=json", function (t) {
              if ((a.empty(), 0 == t.results_count)) a.hide();
              else {
                var e = a.append("<ul></ul>");
                (e = e.find("ul")),
                  $.each(t.results, function (t, n) {
                    if (t >= 6) return !1;
                    var o = $("<a></a>").attr("href", n.url);
                    o.append(
                      '<div class="thumbnail"><img src="' +
                        n.thumbnail +
                        '" /></div>'
                    ),
                      o.append(
                        '<div class="tt-description"><div class="tt-title">' +
                          n.title +
                          "</div></div>"
                      ),
                      o.wrap("<li></li>"),
                      e.append(o.parent());
                  }),
                  t.results_count > 6 &&
                    a.append(
                      '<div class="tt-view-all"><a href="' +
                        o +
                        '">' +
                        seeallresults +
                        " (" +
                        t.results_count +
                        ")</a></div>"
                    ),
                  a.fadeIn(200);
              }
            })));
        });
    }),
      $("body").bind("click", function () {
        $(".search-results").hide();
      });
  }),
  ($('[data-respjs="index_sectiontabs"]').length ||
    $(".tt-carousel-products-js").length ||
    $(".tt-carousel-products").length) &&
    initProductsSlickResize(),
  $(document).ready(function () {
    if (0 == $(".menu_language_holder").length) return !1;
    setTimeout(wgDefault, 100);
  });
var tooltip = {
  html_i: "#tt-tooltip-popup",
  html_s: '<div id="tt-tooltip-popup"><span>',
  html_e: "</span><i></i></div>",
  tp_attr: "[data-tooltip]",
  tp_mod: !1,
  init: function () {
    if (
      ((this.tp_mod = this.get_tp_mod()),
      !this.tp_mod.length ||
        /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(
          navigator.userAgent
        ))
    )
      return !1;
    this.handler();
  },
  get_tp_attr: function () {
    return this.tp_attr;
  },
  get_tp_mod: function () {
    return $(this.get_tp_attr());
  },
  get_w_width: function () {
    return window.innerWidth || $window.width();
  },
  get_html_obj: function (t) {
    return this.html_s + t + this.html_e;
  },
  check_pr_page_swatches: function (t) {
    var e = t.closest(".tt-swatches-container"),
      n = t.closest(".tt-search"),
      o = t.closest(".tt-btn-quickview"),
      a = t.closest(".tt-collapse-content"),
      s = t.closest(".wlbutton-js"),
      r = t.closest(".compare-js");
    return (
      !!e.length ||
      !!n.length ||
      !!o.length ||
      !!a.length ||
      !!s.length ||
      !!r.length
    );
  },
  handler: function () {
    var t = this;
    $("body").on("mouseenter mouseleave", this.get_tp_attr(), function (e) {
      if (1024 >= t.get_w_width()) return !1;
      "mouseenter" === e.type && t.onHover($(this)),
        "mouseleave" === e.type && t.offHover($(this));
    });
  },
  onHover: function (t) {
    var e = this,
      n = t.attr("data-tooltip"),
      o = $(this.get_html_obj(n)),
      a = t.attr("data-tposition"),
      s = t.attr("data-findtag");
    if ("" == n) return !1;
    $body.append(o);
    var r = void 0 !== s ? t.find(s).first() : t,
      c = o.innerHeight(),
      d = o.innerWidth(),
      h = r.innerHeight(),
      u = r.innerWidth(),
      p = r.offset().top,
      f = r.offset().left;
    if (
      ("top" == (a = void 0 !== a ? a : "top") &&
        ((p += -c - 20), (f += parseInt((u - d) / 2))),
      "bottom" == a && ((p += h + 24), (f += parseInt((u - d) / 2))),
      "left" == a && ((p += parseInt((h - c) / 2)), (f += -d - 24)),
      "right" == a && ((p += parseInt((h - c) / 2)), (f += u + 24)),
      this.showTooltip(o, f, p, a),
      !this.check_pr_page_swatches(t))
    )
      return !1;
    t.on("click.closeTooltip", function () {
      e.offHover($(this)), $(this).unbind("click.closeTooltip");
    });
  },
  offHover: function (t) {
    if (($body.find(this.html_i).remove(), !this.check_pr_page_swatches(t)))
      return !1;
    t.unbind("click.closeTooltip");
  },
  showTooltip: function (t, e, n, o) {
    var a = { opacity: 1 },
      s = (o = o || "top");
    "bottom" == s && (s = "top"),
      "right" == s && (s = "left"),
      (a[s] = "bottom" == o || "right" == o ? "-=10px" : "+=10px"),
      t
        .css({ top: n, left: e })
        .addClass("tooltip-" + o)
        .animate(a, 200);
  },
};
$(document).ready(function () {
  tooltip.init();
});
var wl_handler = {
  action_r: "wk-remove",
  action_a: "wk-add",
  loader_t: wokiee_app.loader_text,
  tooltip: "#tt-tooltip-popup",
  noevents: "wkapp_load",
  init: function () {
    if (!$(".wlbutton-js").length) return !1;
    var t = this;
    $("body").on("click", ".wlbutton-js", function (e) {
      if ((e.preventDefault(), $(this).hasClass(t.noevents))) return !1;
      if ("" == t.getcustomerid()) return $("#ModalMessage").modal("show"), !1;
      var n = $(this),
        o = {
          action: n.attr("data-action"),
          text_a: n.attr("data-add"),
          text_r: n.attr("data-remove"),
          namespace: n.attr("data-type"),
          key: n.attr("data-productid"),
          value: n.attr("data-producth"),
        };
      n
        .addClass(t.noevents)
        .attr("data-tooltip", t.loader_t)
        .trigger("mouseenter"),
        o.action == t.action_a ? t.add_handler(n, o) : t.remove_handler(n, o);
    });
  },
  getcustomerid: function () {
    return wokiee_app.main_info.customerid;
  },
  getloadertext: function () {
    return wokiee_app.loader_text;
  },
  getappurl: function () {
    return "https://" + wokiee_app.url;
  },
  add_handler: function (t, e) {
    var n = this;
    $.ajax({
      type: "POST",
      url: n.getappurl() + "/api/add",
      data: $.extend({}, wokiee_app.main_info, {
        namespace: e.namespace,
        key: e.key,
        value: e.value,
      }),
      cache: !1,
      success: function (o) {
        if (
          (console.log("done"),
          t
            .removeClass(n.noevents)
            .attr("data-action", n.action_r)
            .attr("data-tooltip", e.text_r),
          t.hasClass("prpagebtn-js"))
        ) {
          var a = t.attr("data-prpage-rem");
          t.find("span").text(a);
        }
        if (
          (t.is(":hover") && t.trigger("mouseleave").trigger("mouseenter"),
          $(".header-icon-with-text.tt-wishlist").length)
        ) {
          var s = $(".header-icon-with-text.tt-wishlist").find(".tt-badge"),
            r = parseInt(s.text()) + 1;
          s.text(r).show();
        }
      },
      error: function (e, o) {
        alert(
          "Please install ThemePlus App. Link you can find in the documentation"
        ),
          t.removeClass(n.noevents);
      },
    });
  },
  remove_handler: function (t, e) {
    var n = this;
    $.ajax({
      type: "POST",
      url: n.getappurl() + "/api/delete",
      data: $.extend({}, wokiee_app.main_info, {
        namespace: e.namespace,
        key: e.key,
        _method: "DELETE",
      }),
      cache: !1,
      success: function (o) {
        if ((console.log("done"), t.closest(".wkpageinfo").length))
          t.closest(".product-parent")
            .parent()
            .fadeOut(400, function () {
              t.is(":hover") && t.trigger("mouseleave");
              var e = $(this).parent();
              $(this).remove(),
                e.children().length ||
                  e
                    .closest(".wkpageinfo")
                    .parent()
                    .find(".wkpagenoinfo")
                    .removeClass("hide")
                    .parent()
                    .find(".wkpageinfo")
                    .remove();
            });
        else if (
          (t
            .removeClass(n.noevents)
            .attr("data-action", n.action_a)
            .attr("data-tooltip", e.text_a),
          t.is(":hover") && t.trigger("mouseleave").trigger("mouseenter"),
          t.hasClass("prpagebtn-js"))
        ) {
          var a = t.attr("data-prpage-add");
          t.find("span").text(a);
        }
        if ($(".header-icon-with-text.tt-wishlist").length) {
          var s = $(".header-icon-with-text.tt-wishlist").find(".tt-badge"),
            r = Math.max(0, parseInt(s.text()) - 1);
          s.text(r), 0 == r && s.hide();
        }
      },
      error: function (e, o) {
        t.removeClass(n.noevents);
      },
    });
  },
};
wl_handler.init();
var ct = {
    mainc: ".tt-compare-table-js",
    slider: $("#tt-compare-table").find(".compare-init-slider"),
    init: function () {
      if (!$(this.mainc).length) return !1;
      $(window).resize(ct.resizeHandler);
    },
    handler: function () {
      var t = this.getSizes();
      this.setSizes(t),
        this.slider.slick("slickGoTo", 0),
        $(".tt-compare-table").removeAttr("style"),
        (t = !1),
        setTimeout(function () {
          ct.replaceSliderArrows(
            $(".tt-image-box-last").first().position().top
          );
        }, 200);
    },
    setSizes: function (t) {
      $(this.mainc).each(function () {
        $(this)
          .children()
          .each(function (e) {
            $(this).outerHeight(t[e]);
          });
      });
    },
    getSizes: function () {
      var t = [];
      return (
        $(this.mainc).each(function () {
          $(this)
            .children()
            .each(function (e) {
              (t[e] = void 0 !== t[e] ? t[e] : 0),
                (t[e] = Math.max(t[e], $(this).outerHeight()));
            });
        }),
        t
      );
    },
    resizeHandler: function (t) {
      (t = t || 100),
        $(".tt-compare-table").css({ opacity: 0 }),
        $(ct.mainc).children().removeAttr("style"),
        $(ct.mainc).find(".tt-btn-addtocart").removeAttr("style"),
        setTimeout(function () {
          ct.handler();
        }, t);
    },
    replaceSliderArrows: function (t) {
      var e = $(".compare-init-slider").find(".slick-prev"),
        n = $(".compare-init-slider").find(".slick-next"),
        o = parseInt((t - e.height()) / 2);
      e.addClass("showarr").css({ top: o }),
        n.addClass("showarr").css({ top: o });
    },
    sliderinit: function () {
      this.slider.slick({
        dots: !1,
        arrows: !0,
        infinite: !0,
        speed: 300,
        slidesToShow: 3,
        slidesToScroll: 1,
        adaptiveHeight: !0,
        responsive: [
          { breakpoint: 1025, settings: { slidesToShow: 2 } },
          { breakpoint: 790, settings: { slidesToShow: 1 } },
        ],
      });
    },
    sliderunslick: function () {
      this.slider.slick("unslick");
    },
  },
  compare = {
    search: "&view=getproduct",
    a_a: "wk-add",
    a_d: "wk-remove",
    space: "comparedata",
    com_json: {},
    com_count: 0,
    com_limit: 0,
    init: function () {
      var t = this;
      if ((this.comparepage(), !$(".compare-js").length)) return !1;
      this.selected(),
        $(window).on("compareevent", function () {
          var e = $(".compare-js");
          e.length &&
            e
              .attr("data-action", t.a_a)
              .attr("data-tooltip", e.attr("data-add")),
            t.setPageButtonText(e, "data-prpage-add"),
            compare.selected();
        }),
        $("body").on("click", ".compare-js", function (e) {
          e.preventDefault();
          var n = $(this);
          n.attr("data-action") == t.a_a
            ? compare.setdata(n)
            : compare.deletedata(n),
            t.setItemLengthInBadge();
        });
    },
    comparepage: function () {
      if (!$(".comparepage").length || !this.checkEmptyPage()) return !1;
      $(".comparepage").removeClass("hide");
      var t = this,
        e = $.parseJSON(localStorage[compare.space]);
      (this.com_limit = t.getjsonsize(e)),
        $.each(e, function (e, n) {
          t.getproduct(e, n);
        });
    },
    createcomparepage: function (t) {
      var e = $(".compare-init-slider"),
        n = $(".compareitemhtml").find(".tt-item");
      this.setOptionsTable(t, n);
      var o = Object.keys(t).length;
      $(".tt-col-item").attr("data-grid", o),
        $.each(t, function (t, o) {
          var a = n.clone();
          a.find(".tt-remove-item").attr("data-compareid", t),
            a
              .find(".tt-img")
              .find("a")
              .attr("href", o.url)
              .find("img")
              .attr("src", o.image),
            a.find(".tt-title").find("a").text(o.title).attr("href", o.url),
            o.price < o["compare price"]
              ? (a.find(".tt-price").find(".new-price").html(o.price),
                a.find(".tt-price").find(".old-price").html(o["compare price"]))
              : (a
                  .find(".tt-price")
                  .find(".new-price")
                  .html(o.price)
                  .removeClass("new-price"),
                a.find(".tt-price").find(".old-price").remove()),
            Boolean(o.available) ||
              (a.find(".tt-btn-addtocart").closest("form").remove(),
              a.find(".tt-label-location").children().first().remove()),
            a.find("form").find("input").val(t),
            $.each(o.data, function (t, e) {
              var n = a.find('[data-val="' + t + '"]');
              n.length && n.html(e);
            }),
            $.each(o.options, function (t, e) {
              var n = a.find('[data-val="' + t + '"]');
              n.length && n.html(e);
            }),
            e.append(a);
        }),
        $("body").trigger("refreshCurrency"),
        setTimeout(function () {
          $(window).trigger("resize"), ct.sliderinit();
        }, 500);
    },
    checkEmptyPage: function () {
      var t = localStorage[compare.space];
      return (
        !(t = void 0 === t || "undefined" === t) ||
        ($(".comparepageempty").removeClass("hide"),
        $(".comparepage").remove(),
        !1)
      );
    },
    setOptionsTable: function (t, e) {
      var n = $('.tt-value[data-val="empty"]'),
        o = $(".tt-col-title.tt-compare-table-js"),
        a = $('.title-item[data-val="maintable"]'),
        s = [];
      if (
        !a.length ||
        ($.each(t, function (t, e) {
          $.each(e.options, function (t, e) {
            "Title" == t || s.includes(t) || s.push(t);
          });
        }),
        0 == s.length)
      )
        return !1;
      for (var r = 0; r < s.length; r++)
        e.append(n.clone().attr("data-val", s[r])),
          o.append(a.clone().text(s[r]));
    },
    selected: function () {
      var t = localStorage[compare.space];
      if ((t = void 0 === t || "undefined" === t)) return !1;
      var e = this,
        n = $.parseJSON(localStorage[compare.space]);
      $.each(n, function (t, n) {
        var o = $('[data-compareid="' + t + '"]');
        o.length &&
          o
            .attr("data-action", e.a_d)
            .attr("data-tooltip", o.attr("data-remove")),
          e.setPageButtonText(o, "data-prpage-rem");
      });
    },
    setPageButtonText: function (t, e) {
      if (t.hasClass("prpagebtn-js")) {
        var n = t.attr(e);
        t.find("span").text(n);
      }
    },
    getproduct: function (t, e) {
      var n = e + "?variant=" + t + this.search;
      jQuery.ajax({
        type: "get",
        url: n,
        success: function (n) {
          (compare.com_json[t] = $.parseJSON(n)),
            compare.com_json[t].hasOwnProperty("url") &&
              (compare.com_json[t].url = e + "?variant=" + t),
            compare.checkloadwnd();
        },
        error: function (e, n) {
          compare.deleteitem(t),
            compare.checkloadwnd(),
            console.log("product not found");
        },
      });
    },
    checkloadwnd: function () {
      (compare.com_count += 1),
        compare.com_limit <= compare.com_count &&
          compare.createcomparepage(compare.com_json);
    },
    getjsonsize: function (t) {
      return Object.keys(t).length;
    },
    setdata: function (t) {
      var e = localStorage[compare.space],
        n = t.attr("href"),
        o = this.getid(t);
      if ((e = void 0 !== e && "undefined" !== e && e))
        ((e = $.parseJSON(e))[o] = n), (e = JSON.stringify(e));
      else var e = '{"' + o + '": "' + n + '"}';
      (localStorage[compare.space] = e),
        this.setPageButtonText(t, "data-prpage-rem"),
        t
          .attr("data-action", this.a_d)
          .attr("data-tooltip", t.attr("data-remove")),
        t.is(":hover") && t.trigger("mouseleave").trigger("mouseenter");
    },
    getid: function (t) {
      return t.attr("data-compareid");
    },
    deletedata: function (t) {
      var e = this.getid(t),
        n = localStorage[compare.space];
      if (void 0 === n || "undefined" === n)
        return t.attr("data-action", this.a_a), !1;
      (n = $.parseJSON(n)), this.deleteitem(e);
      var o = compare.getjsonsize(n);
      if (n.hasOwnProperty(e) && $(".comparepage").length)
        return (
          (o -= 1),
          t.closest(".tt-item").fadeOut(function () {
            ct.sliderunslick(),
              $('[data-compareid="' + e + '"]').each(function () {
                $(this).closest(".tt-item").remove();
              }),
              $(".tt-col-item").attr("data-grid", o),
              ct.sliderinit(),
              ct.resizeHandler(0),
              compare.checkEmptyPage();
          }),
          !1
        );
      this.setPageButtonText(t, "data-prpage-add"),
        t
          .attr("data-action", this.a_a)
          .attr("data-tooltip", t.attr("data-add")),
        t.is(":hover") && t.trigger("mouseleave").trigger("mouseenter");
    },
    deleteitem: function (t) {
      var e = $.parseJSON(localStorage[compare.space]);
      e.hasOwnProperty(t) &&
        (compare.getjsonsize(e) > 1
          ? (delete e[t],
            (e = JSON.stringify(e)),
            (localStorage[compare.space] = e))
          : localStorage.removeItem(compare.space));
    },
    setItemLengthInBadge: function () {
      var t = localStorage[compare.space];
      if (void 0 === t || "undefined" === t)
        return (
          $(".tt-compare").length && $(".tt-compare").find(".tt-badge").hide(),
          !1
        );
      (t = Object.keys((t = $.parseJSON(t))).length),
        $(".tt-compare").length &&
          $(".tt-compare").find(".tt-badge").show().text(t);
    },
  };
if (
  ($(document).ready(function () {
    compare.init(), compare.setItemLengthInBadge(), ct.init();
  }),
  $(".tostuck-det-js").length &&
    $("body").on("showStuck hideStuck", function (t) {
      "showStuck" == t.type
        ? $(".tostuck-det-js").each(function () {
            var t = $(this).attr("data-stuckparent"),
              n = $(this).attr("data-parent");
            e(t, n, $(this));
          })
        : $(".fromstuck-det-js").each(function () {
            var t = $(this).attr("data-parent");
            e(t, t, $(this));
          });
      function e(t, e, n) {
        if ($("." + t).length) {
          var o = n.children().first().detach();
          $("." + t)
            .attr("data-parent", e)
            .append(o);
        }
      }
    }),
  $(".categories-btn").length)
) {
  function o(t) {
    var e = t.position().top,
      n = t.find(".dropdown-menu");
    if (!n.length) return !1;
    var o = n.height(),
      a = e + t.height(),
      s = t.closest(".headertype2").length ? 0 : -20;
    o >= a ? n.css({ top: s }) : n.css({ top: e + s });
  }
  function a(t, e) {
    if (((e = e || 1), !t.find("svg").length)) return !1;
    var n = t.find("svg").height(),
      o = t.find("span").height(),
      a = t.find("span").position().top - e;
    t.find("svg").css({ top: (o - n) / 2 + a });
  }
  $("body")
    .on("click", ".categories-btn button", function (t) {
      t.preventDefault(),
        $(this)
          .parent()
          .toggleClass("opened")
          .find(".tt-dropdown-menu")
          .slideToggle(200),
        $(".categories-btn nav > ul > li").each(function () {
          o($(this)), a($(this));
        });
    })
    .on("mouseenter", ".categories-btn nav > ul > li", function () {
      o($(this));
    }),
    $(document).ready(function () {
      $(".catmenumob-js").length &&
        $(".catmenumob-js").initMM({
          enable_breakpoint: !0,
          mobile_button: !0,
          breakpoint: 1025,
          menu_class: "mobile-caterorie-menu",
          close_button_name: b_close,
          back_button_name: b_back,
        }),
        $(".mobile-caterorie-menu #mm0 ul li").each(function () {
          a($(this), 2);
        }),
        $(".item-categories").each(function () {
          a($(this), 2);
        });
    });
}
if ($(".icons-main-menu").length) {
  function s(t, e) {
    if (((e = e || 1), !t.find("svg").length)) return !1;
    var n = t.find("svg").height(),
      o = t.find("span").height(),
      a = t.find("span").position().top - e;
    t.find("svg").css({ top: (o - n) / 2 + a });
  }
  $(document).ready(function () {
    $(".catmenumob-js").length &&
      $(".catmenumob-js").initMM({
        enable_breakpoint: !0,
        mobile_button: !0,
        breakpoint: 1025,
        menu_class: "mobile-caterorie-menu",
        close_button_name: b_close,
        back_button_name: b_back,
      }),
      $(".mobile-caterorie-menu #mm0 ul li").each(function () {
        s($(this), 2);
      });
  });
}
function initStickPrLeft() {
  var t = $(".stickprcol-js"),
    e = $(".stickprcolheight-js"),
    n = t.attr("data-o", t.offset().top);
  function o() {
    void 0 !== t.attr("css") && t.removeAttr("css");
    var n = 1 == t.children().length ? t.children().height() : t.height();
    if (n > $(window).height() || n > e.height() || 768 > $(window).width())
      return !1;
    var o = t.attr("data-o"),
      a = (t.position().top, $(window).scrollTop()),
      s = e.height() - n - 10,
      r = Math.max(0, a - o + 68);
    (r = Math.min(s, r)),
      t.css({ top: r }),
      $(".zoomContainer").length && $(".zoomContainer").css({ top: r + 136 });
  }
  $("body").on("click", ".tt-collapse-title", function () {
    setTimeout(function () {
      o();
    }, 200);
  }),
    setTimeout(function () {
      o();
    }, 500),
    $(window)
      .resize(function () {
        t.css({ top: 0 }),
          (n = t.attr("data-o", t.offset().top)),
          setTimeout(function () {
            o();
          }, 100);
      })
      .scroll(function () {
        o();
      });
}
function onerowActiontoInit() {
  if (
    ($(".tt-quantity").find(".tt-col-one").length &&
      $(".tt-quantity")
        .find("a")
        .click(function () {
          $(this).hasClass("tt-col-one")
            ? onerowActiontoRight(!0)
            : onerowActiontoBack();
        }),
    $(window).resize(function () {
      var t = $(".tt-product-listing").hasClass("tt-col-one");
      791 > $(window).width() ? onerowActiontoBack() : onerowActiontoRight(t);
    }),
    $("body").on("listingpageajax", function () {
      if (!$(".tt-product-listing").hasClass("tt-col-one")) return !1;
      onerowActiontoRight(!0);
    }),
    !$(".tt-product-listing").hasClass("tt-col-one"))
  )
    return !1;
  onerowActiontoRight(!0);
}
function onerowActiontoRight(t) {
  if (791 > $(window).width() || !t) return !1;
  $(".product-parent").each(function () {
    if ($(this).find(".onerowactions").children().length) return !0;
    var t = $(this)
      .find(".tt-price")
      .attr("data-par", "backtottprice")
      .wrap('<div class="backtottprice"/>')
      .detach();
    $(this).find(".onerowactions").append(t),
      (t = $(this)
        .find(".tt-product-inside-hover")
        .attr("data-par", "backtorphov")
        .wrap('<div class="backtorphov"/>')
        .detach()),
      $(this).find(".onerowactions").append(t);
  });
}
function onerowActiontoBack() {
  $(".product-parent").each(function () {
    if (!$(this).find(".onerowactions").children().length) return !0;
    var t = $(this).find(".tt-price"),
      e = t.attr("data-par");
    $(this)
      .find("." + e)
      .append(t.detach()),
      $(this).find(".tt-price").unwrap(),
      (e = (t = $(this).find(".tt-product-inside-hover")).attr("data-par")),
      $(this)
        .find("." + e)
        .append(t.detach()),
      $(this).find(".tt-product-inside-hover").unwrap();
  });
}
$(document).ready(function () {
  $(".tt-col-one").length && onerowActiontoInit(),
    $(".stickprcol-js").length && initStickPrLeft(),
    $("[data-currency-selector]").length &&
      $("body").on("click", "[data-currency-selector] a", function (t) {
        t.preventDefault();
        var e = $(this).parent().attr("data-currency");
        $(this)
          .parents("form")
          .find('[name="currency"]')
          .val(e)
          .parents("form")
          .submit();
      });
}),
  $(document).ready(function () {
    function t() {
      var t = $(".tt-stuck-nav");
      if ($(window).width() > 1024) {
        if (t.find(".stuckiconsabsolute").length) return !1;
        t
          .find(".tt-header-row")
          .append('<div class="stuckiconsabsolute"></div>'),
          t
            .find(".tt-parent-box")
            .detach()
            .appendTo(t.find(".stuckiconsabsolute"));
      } else {
        if (!t.find(".stuckiconsabsolute").length) return !1;
        t.find(".tt-parent-box").first().unwrap();
      }
    }
    !(function e() {
      if (
        $("header").hasClass("headertype2") ||
        $("header").hasClass("headertype3")
      )
        return !1;
      var n = $(".tt-stuck-nav");
      if (!n.length) return !1;
      var o = $(".tt-desktop-header");
      return o.find(".tt-obj-menu.obj-aligment-right").length
        ? (n.addClass("stuckmenuinright"), !1)
        : (!!o.find(".menu-in-center").length ||
            !!o.find(".tt-obj-menu.obj-aligment-center").length) &&
            void (n.addClass("stuckmenuincenter"), $(window).resize(t), t());
    })();
  });
var settings = { entryPointInclude: $("#entrypoint-objects") },
  methods = {
    init: function (t) {
      return this.each(function () {
        var e = {
          objClass: $(this).attr("class").split(" ")[0],
          extraPlaceDetach: t.extraPlaceDetach || !1,
          changeСlasStyle: t.changeСlasStyle || !1,
          entryPointInclude: t.entryPointInclude || !1,
          createBlok: t.createBlok,
        };
        window.innerWidth <= 1024 && methods.detachMobile(e, t),
          $(window).resize(function () {
            window.innerWidth <= 1024 && methods.detachMobile(e, t);
          });
      });
    },
    detachMobile: function (t, e) {
      if (
        ($.extend(settings, e),
        t.entryPointInclude.attr("dataDetach", "true"),
        $("header").addClass("dataDetach"),
        t.createBlok)
      ) {
        if (!t.entryPointInclude.find("." + t.createBlok).length) {
          var n = $("." + t.objClass)
            .children()
            .clone()
            .get(0);
          void 0 === n &&
            (n = $("." + t.extraPlaceDetach)
              .children()
              .clone()
              .get(0)),
            t.entryPointInclude
              .append("<div class='external-item " + t.createBlok + "'></div>")
              .find("." + t.createBlok)
              .append(n);
        }
        "false" != typeof t.changeСlasStyle &&
          t.entryPointInclude
            .find("." + t.createBlok)
            .find("." + t.changeСlasStyle)
            .removeClass(t.changeСlasStyle)
            .attr("data-class", t.changeСlasStyle)
            .removeAttr("style");
      }
    },
  };
$.fn.movingObjects = function (t) {
  return methods[t]
    ? methods[t].apply(this, Array.prototype.slice.call(arguments, 1))
    : "object" != typeof t && t
    ? (console.info("Action " + t + "not found this plugin"), this)
    : methods.init.apply(this, arguments);
};
var $header = $("header");
$header.find(".tt-mobile-header-inline").length &&
  ($header
    .find(".tt-desctop-parent-account")
    .movingObjects({
      createBlok: "my-account",
      extraPlaceDetach: "tt-stuck-parent-account",
      changeСlasStyle: "tt-dropdown-menu",
      entryPointInclude: $("#entrypoint-objects"),
    }),
  $header
    .find(".tt-desctop-parent-multi")
    .movingObjects({
      createBlok: "my-multi",
      extraPlaceDetach: "tt-stuck-parent-multi",
      changeСlasStyle: "tt-dropdown-menu",
      entryPointInclude: $("#entrypoint-objects"),
    })),
  (function (t) {
    t("#js-init-menu02").length &&
      (function e() {
        $body.on("click", "#js-init-menu02 .tt-dropdown-toggle", function (e) {
          var n, o, a, s, r, c;
          return (
            (n = t("body").scrollTop() || t("html").scrollTop()),
            $body.css("top", -n),
            (a = (o = t("#js-init-menu02 .tt-modal-header .col-logo")).length
              ? o.position().top
              : 0),
            (s = o.length ? o.innerHeight() / 2 : 0),
            (r = t("#js-init-menu02 .tt-close-modal").innerHeight() / 2),
            (c = parseInt(a + s - r, 10)),
            t("#js-init-menu02")
              .toggleClass("active")
              .find(".tt-close-modal")
              .css("top", n)
              .css("marginTop", c)
              .css("display", "flex")
              .hide()
              .delay(55)
              .fadeIn(150),
            $body.toggleClass("no-scroll notdelete"),
            t("#js-init-menu02 .tt-dropdown-menu").perfectScrollbar(),
            Array.prototype.slice
              .call(document.querySelectorAll("video"))
              .forEach(function (t) {
                t.muted = !0;
              }),
            t("#js-init-menu02")
              .find(".tt-multilev-list li")
              .on("mouseenter", function () {
                t(this)
                  .addClass("active")
                  .closest(".nav-multilevel")
                  .addClass("active")
                  .closest(".tt-dropdown-menu")
                  .addClass("dropdown-menu-active");
                var e = t(this).find("ul:first").addClass("active");
                if (e.length) {
                  e.hasClass("definitionLayout") ||
                    t("body").hasClass("rtl") ||
                    t(this).find("ul:first").css({ marginLeft: -20 });
                  var n =
                    parseInt(t(this).find("> a").innerWidth(), 10) + 17 + 20;
                  t("html").hasClass("rtlstyle")
                    ? t(this).find("> ul").css({ right: n })
                    : t(this).find("> ul").css({ left: n });
                  var o = window.innerWidth,
                    a = parseInt(e.css("width"), 10) + 20,
                    s = this.getBoundingClientRect().right;
                  this.getBoundingClientRect().left,
                    o - s < a
                      ? e.addClass("left-popup")
                      : e.removeClass("left-popup");
                  var r = window.innerHeight,
                    c = parseInt(e.css("height"), 10) + 23,
                    d = this.getBoundingClientRect().top;
                  this.getBoundingClientRect().bottom;
                  var h = c + d,
                    u = parseInt(h - r - 20, 10),
                    p =
                      (parseInt(e.css("height") - 20, 10),
                      parseInt(t(this).find("> a").css("height"), 10)),
                    p = -((p / 2) * 1);
                  if (r < h && r > c) {
                    var u = -1 * u;
                    u < p ? e.css({ top: u }) : e.css({ top: p });
                  } else if (r < h && r < c + 40) {
                    var u = -1 * u;
                    e.css({ top: u });
                  }
                }
                e.stop(!0, !0).addClass("active").delay(200).fadeIn(150);
              })
              .on("mouseleave", function () {
                t(this)
                  .find("ul:first")
                  .stop(!0, !0)
                  .removeClass("active")
                  .fadeOut(50),
                  t(this)
                    .removeClass("active")
                    .closest(".nav-multilevel")
                    .removeClass("active")
                    .closest(".tt-dropdown-menu")
                    .removeClass("dropdown-menu-active"),
                  t(this).find("> ul").removeAttr("style");
              }),
            !1
          );
        }),
          $body.on("click", "#js-init-menu02 .tt-close-modal", function (e) {
            return (
              t("#js-init-menu02").find(".tt-close-modal").fadeOut(190),
              t("#js-init-menu02")
                .find(".tt-dropdown-menu")
                .first()
                .fadeOut(200, function () {
                  t("#js-init-menu02")
                    .find(".tt-dropdown-menu")
                    .first()
                    .removeAttr("style"),
                    t("#js-init-menu02 .tt-dropdown-menu").perfectScrollbar(
                      "destroy"
                    );
                  var e = -1 * parseInt($body.css("top").replace("px", ""), 10);
                  t("#js-init-menu02")
                    .removeClass("active")
                    .find(".tt-close-modal")
                    .delay(0)
                    .fadeOut(0)
                    .removeAttr("style"),
                    $body
                      .removeAttr("style")
                      .removeClass("no-scroll notdelete")
                      .scrollTop(e),
                    $html.removeAttr("style").scrollTop(e),
                    Array.prototype.slice
                      .call(document.querySelectorAll("video"))
                      .forEach(function (t) {
                        t.muted = !1;
                      });
                }),
              !1
            );
          });
        function n() {
          "rtl" == t("body").css("direction") && t("body").addClass("rtl"),
            t("#js-init-menu02 .definitionLayout").each(function () {
              var e = t(this).find(" > li").length;
              e <= 10
                ? t(this).addClass("ul_sm")
                : e >= 11 && e <= 20
                ? t(this).addClass("ul_md").liColl({ c_unit: "%", n_coll: 2 })
                : e >= 21 &&
                  t(this).addClass("ul_lg").liColl({ c_unit: "%", n_coll: 3 });
            });
        }
      })();
  })(jQuery),
  $("[data-linkto]").length &&
    $("[data-linkto]").on("click", function (t) {
      t.preventDefault(), (window.location.href = $(this).attr("data-linkto"));
    }),
  $(".btnanimation").length &&
    $("body")
      .on("mouseenter", ".btnanimation", function (t) {
        if (1025 > $(window).width()) return !1;
        var e = $(this).find(".tt-description-wrapper"),
          n = $(this).find(".tt-description-wrapper-main .custom-buttons");
        if (!n.length) return !1;
        var o = e.height(),
          a =
            1600 > $(window).width()
              ? Number(n.attr("data-offsetmedium"))
              : Number(n.attr("data-offset")),
          s = o + a;
        $(this).hasClass("btnanimationcenter")
          ? ((a += $(this).find(".custom-buttons").height()),
            (a = Math.round(a / 2)),
            n.stop().animate({ top: s, opacity: 1 }, 300),
            e.stop().animate({ top: -a }, 300))
          : ((a += Math.round($(this).find(".custom-buttons").height())),
            e.stop().animate({ top: -a }, 300),
            n.stop().animate({ opacity: 1 }, 600));
      })
      .on("mouseleave", ".btnanimation", function (t) {
        if (1025 > $(window).width()) return !1;
        var e = $(this).find(".tt-description-wrapper"),
          n = $(this).find(".tt-description-wrapper-main .custom-buttons");
        if (!n.length) return !1;
        var o = e.innerHeight();
        if ($(this).hasClass("btnanimationcenter")) {
          var a =
            o -
            (1600 > $(window).width()
              ? Number(n.attr("data-offsetmedium"))
              : Number(n.attr("data-offset"))) +
            10;
          n.stop().animate({ top: a, opacity: 0 }, 200),
            e.stop().animate({ top: 0 }, 300);
        } else e.stop().animate({ top: 0 }, 500), n.stop().animate({ opacity: 0 }, 200);
      }),
  $("[data-customtabsdelay]").length &&
    $("[data-customtabsdelay]").each(function () {
      var t = $(this),
        e = o();
      function n(t) {
        var e = t.children().length - 1,
          n = t.find(".active").parent().index();
        (n += 1) > e && (n = 0), t.children().eq(n).children().trigger("click");
      }
      function o() {
        return setInterval(n, Number(t.attr("data-customtabsdelay")), t);
      }
      t
        .closest(".tabs-box")
        .on("mouseenter", function () {
          clearInterval(e);
        })
        .on("mouseleave", function () {
          e = o();
        }),
        t.find("a").on("mousedown", function () {
          clearInterval(e);
        });
    }),
  $(document).ready(function () {
    var t = "SEE MORE";
    $(".more").each(function () {
      var e = $(this).html();
      if (e.length > 33) {
        var n = e.substr(0, 33),
          o = e.substr(33, e.length - 33);
        $(this).html(
          n +
            '<span class="moreellipses">&nbsp;</span><span class="morecontent"><span>' +
            o +
            '</span>&nbsp;&nbsp;<a href="" class="morelink">' +
            t +
            "</a></span>"
        );
      }
    }),
      $(".morelink").click(function () {
        return (
          $(this).hasClass("less")
            ? ($(this).removeClass("less"), $(this).html(t))
            : ($(this).addClass("less"), $(this).html("SEE LESS")),
          $(this).parent().prev().toggle(),
          $(this).prev().toggle(),
          !1
        );
      });
  }),
  $("body").on("click", ".qtybox .btnqty", function () {
    var t = $(this);
    let e = $(this).attr("data-line");
    var n = $(this).attr("data-index"),
      o = parseInt($(this).parent(".qtybox").find(".quantity-input").val());
    $(this).hasClass("qtyplus") ? o++ : o > 1 && o--,
      (o = isNaN(o) ? 1 : o),
      $.ajax({
        type: "POST",
        url: "/cart/change.js?line=" + e + "&quantity=" + o,
        dataType: "json",
        success: function (e) {
          $.getJSON("/cart.js", function (t) {
            var e = t.item_count;
            $("button.tt-dropdown-toggle span.tt-badge-cart").text(e);
            var n = t.total_price / 100;
            $("body .tt-cart-total-price").text("Rs. " + n + ".00");
            var o = [],
              a = t.items,
              s = 0,
              r = 1;
            for (let c = 0; c < a.length; c++) {
              var d = a[c],
                s = c + 0,
                r = c + 1;
              o.push(
                '<div class="tt-item"> <div class="tt-item-img"> <img src="' +
                  d.image +
                  '"> </div> <div class="tt-item-descriptions"> <h2 class="tt-title">' +
                  d.title +
                  '</h2> <div class="tt-quantity"><span class="qty">' +
                  d.quantity +
                  '</span> X</div> <div class="tt-price">Rs. ' +
                  d.price / 100 +
                  '.00</div> <div class="quantityareaindrawer"> <div class="qtydiv"> <div class="qtybox"> <span data-line="' +
                  r +
                  '" data-index="' +
                  s +
                  '" class="btnqty qtyminus icon icon-minus">-</span> <input type="text" id="quantity" name="properties[quantity]" value="' +
                  d.quantity +
                  '" min="1" class="quantity-selector quantity-input" readonly=""> <span data-line="' +
                  r +
                  '" data-index="' +
                  s +
                  '" class="btnqty qtyplus icon icon-plus">+</span> </div> </div> </div> </div> <div class="tt-item-close"> <a href="/cart/change?id=' +
                  d.id +
                  '&quantity=0" class="tt-btn-close svg-icon-delete header_delete_cartitem_js"> <svg version="1.1" id="Layer_1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" x="0px" y="0px" viewBox="0 0 22 22" style="enable-background:new 0 0 22 22;" xml:space="preserve"> <g> <path d="M3.6,21.1c-0.1-0.1-0.2-0.3-0.2-0.4v-15H2.2C2,5.7,1.9,5.6,1.7,5.5C1.6,5.4,1.6,5.2,1.6,5.1c0-0.2,0.1-0.3,0.2-0.4 C1.9,4.5,2,4.4,2.2,4.4h5V1.9c0-0.2,0.1-0.3,0.2-0.4c0.1-0.1,0.3-0.2,0.4-0.2h6.3c0.2,0,0.3,0.1,0.4,0.2c0.1,0.1,0.2,0.3,0.2,0.4 v2.5h5c0.2,0,0.3,0.1,0.4,0.2c0.1,0.1,0.2,0.3,0.2,0.4c0,0.2-0.1,0.3-0.2,0.4c-0.1,0.1-0.3,0.2-0.4,0.2h-1.3v15 c0,0.2-0.1,0.3-0.2,0.4c-0.1,0.1-0.3,0.2-0.4,0.2H4.1C3.9,21.3,3.7,21.2,3.6,21.1z M17.2,5.7H4.7v14.4h12.5V5.7z M8.3,9 c0.1,0.1,0.2,0.3,0.2,0.4v6.9c0,0.2-0.1,0.3-0.2,0.4c-0.1,0.1-0.3,0.2-0.4,0.2s-0.3-0.1-0.4-0.2c-0.1-0.1-0.2-0.3-0.2-0.4V9.4 c0-0.2,0.1-0.3,0.2-0.4c0.1-0.1,0.3-0.2,0.4-0.2S8.1,8.9,8.3,9z M8.4,4.4h5V2.6h-5V4.4z M11.4,9c0.1,0.1,0.2,0.3,0.2,0.4v6.9 c0,0.2-0.1,0.3-0.2,0.4c-0.1,0.1-0.3,0.2-0.4,0.2s-0.3-0.1-0.4-0.2c-0.1-0.1-0.2-0.3-0.2-0.4V9.4c0-0.2,0.1-0.3,0.2-0.4 c0.1-0.1,0.3-0.2,0.4-0.2S11.3,8.9,11.4,9z M13.6,9c0.1-0.1,0.3-0.2,0.4-0.2s0.3,0.1,0.4,0.2c0.1,0.1,0.2,0.3,0.2,0.4v6.9 c0,0.2-0.1,0.3-0.2,0.4c-0.1,0.1-0.3,0.2-0.4,0.2s-0.3-0.1-0.4-0.2c-0.1-0.1-0.2-0.3-0.2-0.4V9.4C13.4,9.3,13.5,9.1,13.6,9z"></path> </g> </svg> </a> </div> </div>'
              );
            }
            $("body .tt-cart-list").empty(), $("body .tt-cart-list").append(o);
          }),
            $(t).parent().find(".quantity-input").val(e.items[n].quantity),
            $(t)
              .parent()
              .parent()
              .parent()
              .prev()
              .prev()
              .find(".quantity_custom")
              .text(e.items[n].quantity);
        },
        error: function () {},
      });
  }),
  $("body").on("click", ".tt-item-close>a", function () {
    setTimeout(function () {
      $.getJSON("/cart.js", function (t) {
        $("body .tt-cart-list").empty();
        var e = t.item_count;
        $("button.tt-dropdown-toggle span.tt-badge-cart").text(e);
        var n = t.total_price / 100;
        $("body .tt-cart-total-price").text("Rs. " + n + ".00");
        var o = [],
          a = t.items,
          s = 0,
          r = 1;
        for (let c = 0; c < a.length; c++) {
          var d = a[c],
            s = c + 0,
            r = c + 1;
          $("body .tt-cart-list").empty(),
            o.push(
              '<div class="tt-item"> <div class="tt-item-img"> <img src="' +
                d.image +
                '"> </div> <div class="tt-item-descriptions"> <h2 class="tt-title">' +
                d.title +
                '</h2> <div class="tt-quantity"><span class="qty">' +
                d.quantity +
                '</span> X</div> <div class="tt-price">Rs. ' +
                d.price / 100 +
                '.00</div> <div class="quantityareaindrawer"> <div class="qtydiv"> <div class="qtybox"> <span data-line="' +
                r +
                '" data-index="' +
                s +
                '" class="btnqty qtyminus icon icon-minus">-</span> <input type="text" id="quantity" name="properties[quantity]" value="' +
                d.quantity +
                '" min="1" class="quantity-selector quantity-input" readonly=""> <span data-line="' +
                r +
                '" data-index="' +
                s +
                '" class="btnqty qtyplus icon icon-plus">+</span> </div> </div> </div> </div> <div class="tt-item-close"> <a href="/cart/change?id=' +
                d.id +
                '&quantity=0" class="tt-btn-close svg-icon-delete header_delete_cartitem_js"> <svg version="1.1" id="Layer_1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" x="0px" y="0px" viewBox="0 0 22 22" style="enable-background:new 0 0 22 22;" xml:space="preserve"> <g> <path d="M3.6,21.1c-0.1-0.1-0.2-0.3-0.2-0.4v-15H2.2C2,5.7,1.9,5.6,1.7,5.5C1.6,5.4,1.6,5.2,1.6,5.1c0-0.2,0.1-0.3,0.2-0.4 C1.9,4.5,2,4.4,2.2,4.4h5V1.9c0-0.2,0.1-0.3,0.2-0.4c0.1-0.1,0.3-0.2,0.4-0.2h6.3c0.2,0,0.3,0.1,0.4,0.2c0.1,0.1,0.2,0.3,0.2,0.4 v2.5h5c0.2,0,0.3,0.1,0.4,0.2c0.1,0.1,0.2,0.3,0.2,0.4c0,0.2-0.1,0.3-0.2,0.4c-0.1,0.1-0.3,0.2-0.4,0.2h-1.3v15 c0,0.2-0.1,0.3-0.2,0.4c-0.1,0.1-0.3,0.2-0.4,0.2H4.1C3.9,21.3,3.7,21.2,3.6,21.1z M17.2,5.7H4.7v14.4h12.5V5.7z M8.3,9 c0.1,0.1,0.2,0.3,0.2,0.4v6.9c0,0.2-0.1,0.3-0.2,0.4c-0.1,0.1-0.3,0.2-0.4,0.2s-0.3-0.1-0.4-0.2c-0.1-0.1-0.2-0.3-0.2-0.4V9.4 c0-0.2,0.1-0.3,0.2-0.4c0.1-0.1,0.3-0.2,0.4-0.2S8.1,8.9,8.3,9z M8.4,4.4h5V2.6h-5V4.4z M11.4,9c0.1,0.1,0.2,0.3,0.2,0.4v6.9 c0,0.2-0.1,0.3-0.2,0.4c-0.1,0.1-0.3,0.2-0.4,0.2s-0.3-0.1-0.4-0.2c-0.1-0.1-0.2-0.3-0.2-0.4V9.4c0-0.2,0.1-0.3,0.2-0.4 c0.1-0.1,0.3-0.2,0.4-0.2S11.3,8.9,11.4,9z M13.6,9c0.1-0.1,0.3-0.2,0.4-0.2s0.3,0.1,0.4,0.2c0.1,0.1,0.2,0.3,0.2,0.4v6.9 c0,0.2-0.1,0.3-0.2,0.4c-0.1,0.1-0.3,0.2-0.4,0.2s-0.3-0.1-0.4-0.2c-0.1-0.1-0.2-0.3-0.2-0.4V9.4C13.4,9.3,13.5,9.1,13.6,9z"></path> </g> </svg> </a> </div> </div>'
            );
        }
        $("body .tt-cart-list").append(o);
      });
    }, 500);
  }),
  $(".stickyViewCart .textSpan").click(function (t) {
    $(".tt-cart .tt-dropdown-toggle").click();
  });
